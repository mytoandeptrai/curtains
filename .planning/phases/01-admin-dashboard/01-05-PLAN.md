---
phase: 01-admin-dashboard
plan: 05
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - src/modules/category-management/components/category-create-ui/category-create-ui.tsx
  - src/modules/category-management/components/category-edit-ui/category-edit-ui.tsx
  - src/modules/category-management/containers/category-create-container.tsx
  - src/modules/category-management/containers/category-edit-container.tsx
  - src/modules/category-management/hooks/use-category-create.ts
  - src/modules/category-management/hooks/use-category-edit.ts
  - src/modules/product-management/components/product-create-ui/product-create-ui.tsx
  - src/modules/product-management/components/product-edit-ui/product-edit-ui.tsx
  - src/modules/product-management/containers/product-create-container.tsx
  - src/modules/product-management/containers/product-edit-container.tsx
  - src/modules/product-management/hooks/use-product-create.ts
  - src/modules/product-management/hooks/use-product-edit.ts
  - src/modules/lead-management/components/lead-edit-ui/lead-edit-ui.tsx
  - src/modules/lead-management/containers/lead-edit-container.tsx
  - src/modules/lead-management/hooks/use-lead-edit.ts
  - src/modules/booking-management/components/booking-create-ui/booking-create-ui.tsx
  - src/modules/booking-management/components/booking-edit-ui/booking-edit-ui.tsx
  - src/modules/booking-management/containers/booking-create-container.tsx
  - src/modules/booking-management/containers/booking-edit-container.tsx
  - src/modules/booking-management/hooks/use-booking-create.ts
  - src/modules/booking-management/hooks/use-booking-edit.ts
  - src/modules/blog-management/components/blog-create-ui/blog-create-ui.tsx
  - src/modules/blog-management/components/blog-edit-ui/blog-edit-ui.tsx
  - src/modules/blog-management/containers/blog-create-container.tsx
  - src/modules/blog-management/containers/blog-edit-container.tsx
  - src/modules/blog-management/hooks/use-blog-create.ts
  - src/modules/blog-management/hooks/use-blog-edit.ts
  - src/app/admin/categories/create/page.tsx
  - src/app/admin/categories/[id]/edit/page.tsx
  - src/app/admin/products/create/page.tsx
  - src/app/admin/products/[id]/edit/page.tsx
  - src/app/admin/leads/[id]/edit/page.tsx
  - src/app/admin/bookings/create/page.tsx
  - src/app/admin/bookings/[id]/edit/page.tsx
  - src/app/admin/blog/create/page.tsx
  - src/app/admin/blog/[id]/edit/page.tsx
  - src/app/api/email/lead-notification/route.ts
  - src/lib/email.ts
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - PROD-01
  - PROD-02
  - PROD-03
  - PROD-04
  - PROD-05
  - LEAD-08
  - BOOK-01
  - BOOK-02
  - BLOG-01
  - BLOG-02
  - BLOG-03
  - BLOG-04
  - BLOG-05
  - EMAIL-02

must_haves:
  truths:
    - "Admin can create categories via form with validation"
    - "Admin can edit categories via form"
    - "Admin can create products with variants and images"
    - "Admin can edit products, variants, and images"
    - "Admin can edit lead information (name, phone, status, notes)"
    - "Admin can create and edit bookings"
    - "Admin can create and edit blog posts with markdown support"
    - "Email sent to admin when new lead arrives"
    - "Email contains lead info (name, phone, product, estimated price)"
  artifacts:
    - path: src/modules/category-management/components/category-create-ui/category-create-ui.tsx
      provides: "Category create form UI"
      exports: "CategoryCreateUI component"
    - path: src/modules/product-management/components/product-create-ui/product-create-ui.tsx
      provides: "Product create form with variants and images"
      exports: "ProductCreateUI component"
    - path: src/modules/lead-management/components/lead-edit-ui/lead-edit-ui.tsx
      provides: "Lead edit form"
      exports: "LeadEditUI component"
    - path: src/modules/booking-management/components/booking-create-ui/booking-create-ui.tsx
      provides: "Booking create form"
      exports: "BookingCreateUI component"
    - path: src/modules/blog-management/components/blog-create-ui/blog-create-ui.tsx
      provides: "Blog create form with markdown"
      exports: "BlogCreateUI component"
    - path: src/lib/email.ts
      provides: "Email helper functions"
      exports: "sendLeadNotificationEmail"
    - path: src/app/api/email/lead-notification/route.ts
      provides: "Email sending endpoint"
      exports: "POST handler"
  key_links:
    - from: src/modules/product-management/hooks/use-product-create.ts
      to: src/lib/utils/price-calculator.ts
      via: "Calculate price for display in form"
      pattern: "calculatePrice"
    - from: src/app/api/email/lead-notification/route.ts
      to: src/lib/email.ts
      via: "Send email via Resend service"
      pattern: "sendLeadNotificationEmail"

---

<objective>
Implement CRUD forms for all admin modules (create/edit pages) and email notification system for new leads.

Purpose: Allow admin to create and edit all content types; send email alerts on new leads
Output: Form components, pages, and email service
</objective>

<execution_context>
@/Users/macbookpro/Desktop/Projects/curtains/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/01-admin-dashboard/01-01-SUMMARY.md
@.planning/phases/01-admin-dashboard/01-03-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/1-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CRUD forms for categories, products, leads, bookings, blog</name>
  <files>
    - src/modules/category-management/components/category-create-ui/category-create-ui.tsx
    - src/modules/category-management/components/category-edit-ui/category-edit-ui.tsx
    - src/modules/category-management/containers/category-create-container.tsx
    - src/modules/category-management/containers/category-edit-container.tsx
    - src/modules/category-management/hooks/use-category-create.ts
    - src/modules/category-management/hooks/use-category-edit.ts
    - src/modules/product-management/components/product-create-ui/product-create-ui.tsx
    - src/modules/product-management/components/product-edit-ui/product-edit-ui.tsx
    - src/modules/product-management/containers/product-create-container.tsx
    - src/modules/product-management/containers/product-edit-container.tsx
    - src/modules/product-management/hooks/use-product-create.ts
    - src/modules/product-management/hooks/use-product-edit.ts
    - src/modules/lead-management/components/lead-edit-ui/lead-edit-ui.tsx
    - src/modules/lead-management/containers/lead-edit-container.tsx
    - src/modules/lead-management/hooks/use-lead-edit.ts
    - src/modules/booking-management/components/booking-create-ui/booking-create-ui.tsx
    - src/modules/booking-management/components/booking-edit-ui/booking-edit-ui.tsx
    - src/modules/booking-management/containers/booking-create-container.tsx
    - src/modules/booking-management/containers/booking-edit-container.tsx
    - src/modules/booking-management/hooks/use-booking-create.ts
    - src/modules/booking-management/hooks/use-booking-edit.ts
    - src/modules/blog-management/components/blog-create-ui/blog-create-ui.tsx
    - src/modules/blog-management/components/blog-edit-ui/blog-edit-ui.tsx
    - src/modules/blog-management/containers/blog-create-container.tsx
    - src/modules/blog-management/containers/blog-edit-container.tsx
    - src/modules/blog-management/hooks/use-blog-create.ts
    - src/modules/blog-management/hooks/use-blog-edit.ts
  </files>
  <action>
Create create/edit form components for all modules. Each module gets separate create and edit UI components (not shared, per D-05 from CONTEXT.md).

**Pattern for all forms (example: CategoryCreateUI):**

```typescript
'use client';

import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { Form } from '@/components/ui/form';
import { Button } from '@/components/ui/button';
import { FormInput } from '@/components/form-fields/form-input';
import { FormTextarea } from '@/components/form-fields/form-textarea';
import { categoryCreateSchema } from '@/lib/schemas/category';

interface CategoryCreateUIProps {
  onSubmit: (data: any) => Promise<void>;
  isLoading: boolean;
}

export function CategoryCreateUI({ onSubmit, isLoading }: CategoryCreateUIProps) {
  const form = useForm({
    resolver: zodResolver(categoryCreateSchema),
    defaultValues: {
      name: '',
      slug: '',
      description: '',
      seo_title: '',
      seo_description: '',
    },
  });

  return (
    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-6">
        <FormInput
          control={form.control}
          name="name"
          label="Category Name"
          placeholder="e.g., Roller Blinds"
          required
        />

        <FormInput
          control={form.control}
          name="slug"
          label="Slug"
          placeholder="e.g., roller-blinds"
          required
        />

        <FormTextarea
          control={form.control}
          name="description"
          label="Description"
          placeholder="Category description"
        />

        <div className="grid gap-4 md:grid-cols-2">
          <FormInput
            control={form.control}
            name="seo_title"
            label="SEO Title"
            placeholder="For search engines"
          />

          <FormInput
            control={form.control}
            name="seo_description"
            label="SEO Description"
            placeholder="For search engines"
          />
        </div>

        <div className="flex gap-2">
          <Button type="submit" disabled={isLoading}>
            {isLoading ? 'Creating...' : 'Create Category'}
          </Button>
        </div>
      </form>
    </Form>
  );
}
```

**Hook pattern (use-category-create.ts):**
```typescript
'use client';

import { useRouter } from 'next/navigation';
import { useState } from 'react';
import { toast } from 'sonner';

export function useCategoryCreate() {
  const router = useRouter();
  const [isLoading, setIsLoading] = useState(false);

  const onSubmit = async (data: any) => {
    setIsLoading(true);
    try {
      const response = await fetch('/api/admin/categories', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data),
      });

      if (!response.ok) {
        const error = await response.json();
        throw new Error(error.error || 'Failed to create');
      }

      toast.success('Category created successfully');
      router.push('/admin/categories');
    } catch (error: any) {
      toast.error(error.message || 'Failed to create category');
    } finally {
      setIsLoading(false);
    }
  };

  return { onSubmit, isLoading };
}
```

**Container pattern:**
```typescript
'use client';

import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { useCategoryCreate } from '../hooks/use-category-create';
import { CategoryCreateUI } from '../components/category-create-ui/category-create-ui';
import { categoryCreateSchema } from '@/lib/schemas/category';

export function CategoryCreateContainer() {
  const form = useForm({
    resolver: zodResolver(categoryCreateSchema),
    defaultValues: { /* ... */ },
  });

  const { onSubmit, isLoading } = useCategoryCreate();

  return (
    <CategoryCreateUI
      onSubmit={form.handleSubmit(onSubmit)}
      isLoading={isLoading}
    />
  );
}
```

**Domain-specific notes:**

**Product Forms:**
- Show base_price (number)
- Show category_id (dropdown of categories)
- Show featured (checkbox)
- Show extras (array input: type, amount, name)
- Image upload is separate (done in Phase 1.5 with modal)
- Variant management is separate

**Lead Forms (edit only):**
- Show name, phone, address (text inputs)
- Show status dropdown (new, contacted, closed)
- Show notes (textarea)
- Show estimated_price (read-only display)
- Show created_at, product_id (read-only)

**Booking Forms:**
- Show lead_id (select/dropdown)
- Show booking_date (date picker)
- Show booking_time (time picker)
- Show status (dropdown: pending, confirmed, done)
- Show notes (textarea)

**Blog Forms:**
- Show title, slug (text inputs)
- Show content (textarea, markdown support)
- Show excerpt (textarea)
- Show seo_title, seo_description, seo_keywords
- Show published (checkbox)

All forms use:
- React Hook Form + Zod validation
- Shared form field components (FormInput, FormTextarea, FormSelect, etc.)
- Toast notifications for success/error
- Loading state during submission
- Redirect to list page on success
  </action>
  <verify>
    <automated>find src/modules -name "*-create-ui.tsx" -o -name "*-edit-ui.tsx" | wc -l should be >= 14</automated>
  </verify>
  <done>All CRUD form components created for categories, products, leads, bookings, and blog</done>
</task>

<task type="auto">
  <name>Task 2: Create form pages (create/edit routes)</name>
  <files>
    - src/app/admin/categories/create/page.tsx
    - src/app/admin/categories/[id]/edit/page.tsx
    - src/app/admin/products/create/page.tsx
    - src/app/admin/products/[id]/edit/page.tsx
    - src/app/admin/leads/[id]/edit/page.tsx
    - src/app/admin/bookings/create/page.tsx
    - src/app/admin/bookings/[id]/edit/page.tsx
    - src/app/admin/blog/create/page.tsx
    - src/app/admin/blog/[id]/edit/page.tsx
  </files>
  <action>
Create page routes for all create/edit forms. Pages are minimal — just import and render the container.

**Pattern (example: CategoryCreatePage):**
```typescript
import { CategoryCreateContainer } from '@/modules/category-management';

export const metadata = {
  title: 'Create Category',
};

export default function CreateCategoryPage() {
  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-3xl font-bold">Create Category</h1>
        <p className="text-gray-600">Add a new product category</p>
      </div>

      <CategoryCreateContainer />
    </div>
  );
}
```

**For edit pages with dynamic [id]:**
```typescript
import { CategoryEditContainer } from '@/modules/category-management';

export const metadata = {
  title: 'Edit Category',
};

export default function EditCategoryPage({ params }: { params: { id: string } }) {
  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-3xl font-bold">Edit Category</h1>
        <p className="text-gray-600">Update category information</p>
      </div>

      <CategoryEditContainer id={params.id} />
    </div>
  );
}
```

Note: Edit containers must accept `id` prop to fetch existing data before rendering form.

Create all 9 pages with appropriate titles and descriptions for each domain.
  </action>
  <verify>
    <automated>find src/app/admin -name "create/page.tsx" -o -name "edit/page.tsx" | wc -l should be >= 9</automated>
  </verify>
  <done>All form pages created (create and edit routes)</done>
</task>

<task type="auto">
  <name>Task 3: Create email service and lead notification endpoint</name>
  <files>
    - src/lib/email.ts
    - src/app/api/email/lead-notification/route.ts
  </files>
  <action>
Create email service using Resend API (per D-01 from CONTEXT.md). Admin receives email notification when new lead arrives.

**src/lib/email.ts:**
```typescript
import { Resend } from 'resend';

const resend = new Resend(process.env.RESEND_API_KEY);

export interface LeadNotificationData {
  lead_id: string;
  lead_name: string;
  lead_phone: string;
  lead_address: string;
  product_name: string;
  estimated_price: number;
  created_at: string;
}

/**
 * Send email notification to admin when new lead arrives
 */
export async function sendLeadNotificationEmail(data: LeadNotificationData) {
  const adminEmail = process.env.ADMIN_EMAIL || 'admin@example.com';

  const emailHtml = `
    <h2>New Lead Received</h2>
    <p>A new lead has been submitted. Here are the details:</p>
    <ul>
      <li><strong>Name:</strong> ${data.lead_name}</li>
      <li><strong>Phone:</strong> ${data.lead_phone}</li>
      <li><strong>Address:</strong> ${data.lead_address}</li>
      <li><strong>Product:</strong> ${data.product_name}</li>
      <li><strong>Estimated Price:</strong> ${new Intl.NumberFormat('vi-VN', {
        style: 'currency',
        currency: 'VND',
        maximumFractionDigits: 0,
      }).format(data.estimated_price)}</li>
      <li><strong>Submitted:</strong> ${new Date(data.created_at).toLocaleString('vi-VN')}</li>
    </ul>
    <p>
      <a href="${process.env.NEXT_PUBLIC_APP_URL}/admin/leads">View in admin dashboard</a>
    </p>
  `;

  try {
    const response = await resend.emails.send({
      from: process.env.RESEND_FROM_EMAIL || 'noreply@curtains.com',
      to: adminEmail,
      subject: `New Lead: ${data.lead_name}`,
      html: emailHtml,
    });

    if (response.error) {
      throw new Error(response.error.message);
    }

    return response;
  } catch (error) {
    console.error('Failed to send lead notification email:', error);
    throw error;
  }
}

/**
 * Send confirmation email to customer
 * (prepared for Phase 2 when leads come from public form)
 */
export async function sendLeadConfirmationEmail(
  customerEmail: string,
  customerName: string,
  data: LeadNotificationData
) {
  const emailHtml = `
    <h2>Lead Submission Confirmed</h2>
    <p>Thank you ${customerName} for your interest in our products!</p>
    <p>We have received your request for <strong>${data.product_name}</strong>.</p>
    <p><strong>Estimated price:</strong> ${new Intl.NumberFormat('vi-VN', {
      style: 'currency',
      currency: 'VND',
      maximumFractionDigits: 0,
    }).format(data.estimated_price)}</p>
    <p>Our team will contact you shortly at ${data.lead_phone}.</p>
    <p>Thank you!</p>
  `;

  try {
    const response = await resend.emails.send({
      from: process.env.RESEND_FROM_EMAIL || 'noreply@curtains.com',
      to: customerEmail,
      subject: `Confirmation: Your ${data.product_name} Inquiry`,
      html: emailHtml,
    });

    if (response.error) {
      throw new Error(response.error.message);
    }

    return response;
  } catch (error) {
    console.error('Failed to send lead confirmation email:', error);
    throw error;
  }
}
```

**src/app/api/email/lead-notification/route.ts:**
```typescript
import { NextRequest, NextResponse } from 'next/server';
import { sendLeadNotificationEmail, LeadNotificationData } from '@/lib/email';

/**
 * Internal endpoint to send lead notification email
 * Called from POST /api/admin/leads or Phase 2 form submission
 */
export async function POST(request: NextRequest) {
  try {
    const body = await request.json() as LeadNotificationData;

    // Validate required fields
    if (!body.lead_name || !body.lead_phone || !body.product_name) {
      return NextResponse.json(
        { error: 'Missing required fields' },
        { status: 400 }
      );
    }

    // Send email
    const result = await sendLeadNotificationEmail(body);

    return NextResponse.json({
      success: true,
      message: 'Email sent successfully',
      emailId: result.id,
    });
  } catch (error: any) {
    console.error('Email sending failed:', error);
    return NextResponse.json(
      { error: error.message || 'Failed to send email' },
      { status: 500 }
    );
  }
}
```

**Environment variables needed (add to .env.local):**
```
RESEND_API_KEY=re_4BeEpsAZ_GYw7peT2yvujjcA1p5GM67js
RESEND_FROM_EMAIL=noreply@curtains.com
ADMIN_EMAIL=admin@example.com
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

**Integration point for Phase 1.5 (when creating leads from admin):**
When admin creates a new lead via /api/admin/leads POST, call the email endpoint:
```typescript
// In create lead hook or API route
await fetch('/api/email/lead-notification', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    lead_id: newLead.id,
    lead_name: newLead.name,
    lead_phone: newLead.phone,
    lead_address: newLead.address,
    product_name: product.name,
    estimated_price: newLead.estimated_price,
    created_at: newLead.created_at,
  }),
});
```

Key notes:
- Resend is email-only (no SMS in Phase 1; Zalo API deferred to v2)
- Email templates are basic HTML (can enhance in Phase 2)
- Admin email from environment variable
- Customer confirmation prepared for Phase 2
- All errors logged to console (no UI toast for background task)
  </action>
  <verify>
    <automated>grep -c "sendLeadNotificationEmail\|RESEND_API_KEY" src/lib/email.ts src/app/api/email/lead-notification/route.ts should be >= 2</automated>
  </verify>
  <done>Email service created with lead notification and customer confirmation templates</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form → API | Form input validated by Zod at form level and API level |
| API → Database | Writes checked for authorization and constraint violations |
| Email Service → Resend | API key in .env.local (never exposed to frontend) |
| Email → Admin | Contains PII (lead name, phone); requires authenticated recipient |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-001 | Tampering | Form Input | Mitigate | Zod validation at form + API entry point |
| T-05-002 | Information Disclosure | Lead Email | Mitigate | Admin email is authenticated; email sent over HTTPS via Resend |
| T-05-003 | Repudiation | Email Sending | Mitigate | Resend tracks email delivery; failures logged to console |
| T-05-004 | Denial of Service | Email Endpoint | Accept | No rate limiting (internal use only) |
| T-05-005 | Elevation of Privilege | Create Lead | Mitigate | API checks session before creating; requires admin auth |

</threat_model>

<verification>
Phase 1.5 (Forms & Email) is complete when:
- [ ] All create/edit forms render without errors
- [ ] Forms validate input correctly (Zod)
- [ ] Form submission creates/updates records via API
- [ ] Success/error toasts display after submission
- [ ] Create forms redirect to list on success
- [ ] Edit forms load existing data and pre-fill form
- [ ] Email endpoint sends notification when lead created
- [ ] Email contains all required info (name, phone, product, price)
- [ ] No TypeScript errors in form components
</verification>

<success_criteria>
- Create category form submits and creates new category
- Edit category form loads existing data and updates
- Product form includes variants and extras management
- Lead form shows status dropdown and notes
- Blog form accepts markdown content
- Email notification sent to admin with lead details
- Form pages appear in sidebar navigation (via links from list pages)
- All form validations work (required fields, format checks)
</success_criteria>

<output>
After completion, create `.planning/phases/01-admin-dashboard/01-05-SUMMARY.md` documenting:
- Form patterns (create/edit UI, containers, hooks)
- Form validation using Zod
- Email service integration with Resend
- Lead notification template
</output>
