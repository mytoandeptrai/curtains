---
phase: 02-platform-portal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/products/index.ts
  - src/api/products/keys.ts
  - src/api/products/types.ts
  - src/api/products/requests.ts
  - src/api/products/queries.ts
  - src/api/leads/index.ts
  - src/api/leads/keys.ts
  - src/api/leads/types.ts
  - src/api/leads/requests.ts
  - src/api/leads/queries.ts
  - src/api/bookings/index.ts
  - src/api/bookings/keys.ts
  - src/api/bookings/types.ts
  - src/api/bookings/requests.ts
  - src/api/bookings/queries.ts
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03, PROD-04, PROD-05, PROD-06, PROD-07, PROD-08, LEAD-07, LEAD-08, LEAD-09, LEAD-10, BOOK-01, BOOK-02, BOOK-03, BOOK-04, BOOK-05]
must_haves:
  truths:
    - "Products API module created using category pattern (TanStack Query + HTTP instance)"
    - "Leads API module created using category pattern"
    - "Bookings API module created using category pattern"
    - "All three modules fully typed and ready for container refactor"
    - "Pattern consistently replicated across three modules"
  artifacts:
    - path: "src/api/products/queries.ts"
      provides: "Product query/mutation hooks"
      exports: ["useGetProductList", "useGetProductDetail", "useCreateProductMutation", "useUpdateProductMutation", "useDeleteProductMutation"]
    - path: "src/api/leads/queries.ts"
      provides: "Lead query/mutation hooks"
      exports: ["useGetLeadList", "useGetLeadDetail", "useCreateLeadMutation", "useUpdateLeadMutation", "useDeleteLeadMutation"]
    - path: "src/api/bookings/queries.ts"
      provides: "Booking query/mutation hooks"
      exports: ["useGetBookingList", "useGetBookingDetail", "useCreateBookingMutation", "useUpdateBookingMutation", "useDeleteBookingMutation"]
  key_links:
    - from: "src/api/products/queries.ts"
      to: "src/api/categories/queries.ts"
      via: "pattern replication"
      pattern: "Hook signature structure matches category pattern"
    - from: "src/api/leads/queries.ts"
      to: "src/api/categories/queries.ts"
      via: "pattern replication"
      pattern: "Hook signature structure matches category pattern"
    - from: "src/api/bookings/queries.ts"
      to: "src/api/categories/queries.ts"
      via: "pattern replication"
      pattern: "Hook signature structure matches category pattern"
---

<objective>
Replicate the category API pattern across three more modules: products, leads, bookings. This ensures consistent architecture across all admin modules before Wave 2 refactors containers and forms.

Purpose: Products, leads, and bookings are more complex than categories (products have variants/images, leads have leads flow, bookings have scheduling), but the API query/mutation pattern remains identical.

Output:
- src/api/products/ complete module (TanStack Query + HTTP instance)
- src/api/leads/ complete module (TanStack Query + HTTP instance)
- src/api/bookings/ complete module (TanStack Query + HTTP instance)
- All modules follow exact category pattern structure for consistency
</objective>

<execution_context>
@/Users/macbookpro/Desktop/Projects/curtains/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/macbookpro/Desktop/Projects/curtains/.planning/phases/02-platform-portal/02-CONTEXT.md
@/Users/macbookpro/Desktop/Projects/curtains/docs/requirements/refactor-phase-1.md
@/Users/macbookpro/Desktop/Projects/curtains/.planning/phases/02-platform-portal/02-01-PLAN.md
</context>

<interfaces>
From src/api/categories/types.ts (completed in Plan 01):
```typescript
export interface ICategory {
  id: string;
  name: string;
  slug: string;
  description: string;
  seo_title: string;
  seo_description: string;
  created_at: string;
  updated_at: string;
}

export interface GetCategoryListParams {
  page?: number;
  pageSize?: number;
  sortBy?: string;
  orderBy?: 'asc' | 'desc';
  search?: string;
}
```

From src/modules/product-management/hooks/use-product-list.ts (Phase 1 current):
```typescript
export interface Product {
  id: string;
  name: string;
  slug: string;
  description: string;
  price: number;
  category: string;
  image: string;
  featured: boolean;
  created_at: string;
}
```

From src/modules/lead-management/hooks/use-lead-list.ts (Phase 1 current):
```typescript
export interface Lead {
  id: string;
  customer_name: string;
  customer_email: string;
  customer_phone: string;
  status: 'new' | 'contacted' | 'closed';
  product_id: string;
  created_at: string;
}
```

From src/modules/booking-management/hooks/use-booking-list.ts (Phase 1 current):
```typescript
export interface Booking {
  id: string;
  lead_id: string;
  booking_date: string;
  booking_time: string;
  status: 'pending' | 'confirmed' | 'done';
  notes: string;
  created_at: string;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create products API module (types, keys, requests, queries)</name>
  <files>
    - src/api/products/types.ts
    - src/api/products/keys.ts
    - src/api/products/requests.ts
    - src/api/products/queries.ts
    - src/api/products/index.ts
  </files>
  <read_first>
  - src/api/categories/ (complete directory to use as pattern template)
  - src/modules/product-management/hooks/use-product-list.ts (current Product interface)
  - src/types/index.ts (for BaseResponseType, IPaginatedResponseType)
  </read_first>
  <action>
  Create products API module by replicating category module structure with product-specific types and endpoints:

  **src/api/products/types.ts:**
  - IProduct interface with fields: id, name, slug, description, price, category, image, featured, created_at, updated_at
  - GetProductListParams with page, pageSize, sortBy, orderBy, search, category (optional), featured (optional)
  - CreateProductParams with name, slug, description, price, category, featured (optional), images (optional)
  - UpdateProductParams extends CreateProductParams + { id }
  - DeleteProductParams with id
  - GetProductListResponse extends IPaginatedResponseType<IProduct[]>
  - CreateProductResponse, UpdateProductResponse, DeleteProductResponse extend BaseResponseType

  **src/api/products/keys.ts:**
  - KEYS object with: PRODUCTS, PRODUCT_DETAIL, CREATE_PRODUCT, UPDATE_PRODUCT, DELETE_PRODUCT
  - Endpoint paths: 'admin/products', 'admin/products/:id', etc.

  **src/api/products/requests.ts:**
  - getProductList, getProductDetail, createProduct, updateProduct, deleteProduct functions
  - Pattern: Exactly replicate category requests.ts with product endpoints and types

  **src/api/products/queries.ts:**
  - useGetProductList, useGetProductDetail, useCreateProductMutation, useUpdateProductMutation, useDeleteProductMutation hooks
  - Pattern: Exactly replicate category queries.ts

  **src/api/products/index.ts:**
  - Barrel exports: export * from './keys'; export * from './types'; export * from './requests'; export * from './queries';

  Use category module as reference for exact structure and signature format.
  </action>
  <verify>
    <automated>grep -c "export const useGetProductList\|export const useCreateProductMutation\|export const useUpdateProductMutation\|export const useDeleteProductMutation\|export const useGetProductDetail" /Users/macbookpro/Desktop/Projects/curtains/src/api/products/queries.ts</automated>
  </verify>
  <done>Products API module complete with all 5 hooks and 5 files</done>
</task>

<task type="auto">
  <name>Task 2: Create leads API module (types, keys, requests, queries)</name>
  <files>
    - src/api/leads/types.ts
    - src/api/leads/keys.ts
    - src/api/leads/requests.ts
    - src/api/leads/queries.ts
    - src/api/leads/index.ts
  </files>
  <read_first>
  - src/api/categories/ (pattern template)
  - src/modules/lead-management/hooks/use-lead-list.ts (current Lead interface)
  - src/types/index.ts (for response types)
  </read_first>
  <action>
  Create leads API module by replicating category module structure with lead-specific types and endpoints:

  **src/api/leads/types.ts:**
  - ILead interface with fields: id, customer_name, customer_email, customer_phone, status, product_id, notes (optional), created_at, updated_at
  - Lead status enum: 'new' | 'contacted' | 'closed'
  - GetLeadListParams with page, pageSize, sortBy, orderBy, search, status (optional), product_id (optional)
  - CreateLeadParams with customer_name, customer_email, customer_phone, product_id, status (optional), notes (optional)
  - UpdateLeadParams extends CreateLeadParams + { id }
  - DeleteLeadParams with id
  - GetLeadListResponse extends IPaginatedResponseType<ILead[]>

  **src/api/leads/keys.ts:**
  - KEYS object with: LEADS, LEAD_DETAIL, CREATE_LEAD, UPDATE_LEAD, DELETE_LEAD
  - Endpoint paths: 'admin/leads', 'admin/leads/:id', etc.

  **src/api/leads/requests.ts:**
  - getLeadList, getLeadDetail, createLead, updateLead, deleteLead functions
  - Pattern: Exactly replicate category requests.ts

  **src/api/leads/queries.ts:**
  - useGetLeadList, useGetLeadDetail, useCreateLeadMutation, useUpdateLeadMutation, useDeleteLeadMutation hooks
  - Pattern: Exactly replicate category queries.ts

  **src/api/leads/index.ts:**
  - Barrel exports (4 lines)

  Use category module as reference.
  </action>
  <verify>
    <automated>grep -c "export const useGetLeadList\|export const useCreateLeadMutation\|export const useUpdateLeadMutation\|export const useDeleteLeadMutation\|export const useGetLeadDetail" /Users/macbookpro/Desktop/Projects/curtains/src/api/leads/queries.ts</automated>
  </verify>
  <done>Leads API module complete with all 5 hooks and 5 files</done>
</task>

<task type="auto">
  <name>Task 3: Create bookings API module (types, keys, requests, queries)</name>
  <files>
    - src/api/bookings/types.ts
    - src/api/bookings/keys.ts
    - src/api/bookings/requests.ts
    - src/api/bookings/queries.ts
    - src/api/bookings/index.ts
  </files>
  <read_first>
  - src/api/categories/ (pattern template)
  - src/modules/booking-management/hooks/use-booking-list.ts (current Booking interface)
  - src/types/index.ts (for response types)
  </read_first>
  <action>
  Create bookings API module by replicating category module structure with booking-specific types and endpoints:

  **src/api/bookings/types.ts:**
  - IBooking interface with fields: id, lead_id, booking_date, booking_time, status, notes (optional), created_at, updated_at
  - Booking status enum: 'pending' | 'confirmed' | 'done'
  - GetBookingListParams with page, pageSize, sortBy, orderBy, search, status (optional), lead_id (optional)
  - CreateBookingParams with lead_id, booking_date, booking_time, status (optional), notes (optional)
  - UpdateBookingParams extends CreateBookingParams + { id }
  - DeleteBookingParams with id
  - GetBookingListResponse extends IPaginatedResponseType<IBooking[]>

  **src/api/bookings/keys.ts:**
  - KEYS object with: BOOKINGS, BOOKING_DETAIL, CREATE_BOOKING, UPDATE_BOOKING, DELETE_BOOKING
  - Endpoint paths: 'admin/bookings', 'admin/bookings/:id', etc.

  **src/api/bookings/requests.ts:**
  - getBookingList, getBookingDetail, createBooking, updateBooking, deleteBooking functions
  - Pattern: Exactly replicate category requests.ts

  **src/api/bookings/queries.ts:**
  - useGetBookingList, useGetBookingDetail, useCreateBookingMutation, useUpdateBookingMutation, useDeleteBookingMutation hooks
  - Pattern: Exactly replicate category queries.ts

  **src/api/bookings/index.ts:**
  - Barrel exports (4 lines)

  Use category module as reference.
  </action>
  <verify>
    <automated>grep -c "export const useGetBookingList\|export const useCreateBookingMutation\|export const useUpdateBookingMutation\|export const useDeleteBookingMutation\|export const useGetBookingDetail" /Users/macbookpro/Desktop/Projects/curtains/src/api/bookings/queries.ts</automated>
  </verify>
  <done>Bookings API module complete with all 5 hooks and 5 files</done>
</task>

<task type="auto">
  <name>Task 4: Verify TypeScript compilation for all three modules</name>
  <files>src/api/products/, src/api/leads/, src/api/bookings/</files>
  <read_first>
  - src/api/products/index.ts (to verify barrel exports)
  - src/api/leads/index.ts (to verify barrel exports)
  - src/api/bookings/index.ts (to verify barrel exports)
  - tsconfig.json
  </read_first>
  <action>
  Run TypeScript type-check to ensure all newly created modules compile without errors:

  ```bash
  cd /Users/macbookpro/Desktop/Projects/curtains && npx tsc --noEmit
  ```

  This verifies:
  - All imports in products, leads, bookings modules resolve correctly
  - Type definitions are valid
  - No missing exports or circular dependencies
  - All TanStack Query type signatures are correct
  </action>
  <verify>
    <automated>cd /Users/macbookpro/Desktop/Projects/curtains && npx tsc --noEmit 2>&1 | grep -q "error" && exit 1 || exit 0</automated>
  </verify>
  <done>TypeScript compilation passes for all three modules with no errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | HTTP requests to /admin/products, /admin/leads, /admin/bookings (authenticated) |
| API → Database | Query/mutation data from Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Injection | src/api/products/requests.ts | mitigate | Use httpInstance with typed payloads (validation in containers) |
| T-02-06 | Injection | src/api/leads/requests.ts | mitigate | Use httpInstance with typed payloads |
| T-02-07 | Injection | src/api/bookings/requests.ts | mitigate | Use httpInstance with typed payloads |
| T-02-08 | Authentication | All modules | mitigate | HTTP instance middleware enforces session auth |
| T-02-09 | Authorization | All modules | mitigate | Backend RLS policies restrict to authenticated admins |

</threat_model>

<verification>
After task completion:
1. ✓ File src/api/products/index.ts exports types, keys, requests, queries
2. ✓ File src/api/leads/index.ts exports types, keys, requests, queries
3. ✓ File src/api/bookings/index.ts exports types, keys, requests, queries
4. ✓ All modules export 5 hooks each (useGetList, useGetDetail, useCreate, useUpdate, useDelete mutations)
5. ✓ All modules use httpInstance (no fetch keyword)
6. ✓ Pattern structure consistently matches category module across all three
7. ✓ npx tsc --noEmit passes with no errors
</verification>

<success_criteria>
Phase 2 Plan 02 is complete when:
- All 3 modules exist with complete structure (types, keys, requests, queries, index files)
- Each module exports 5 hooks following TanStack Query pattern
- TypeScript compilation passes
- Pattern consistency verified across categories, products, leads, bookings
- Ready for Wave 2 to refactor containers and forms using these hooks
</success_criteria>

<output>
After completion, create `.planning/phases/02-platform-portal/02-02-SUMMARY.md`
</output>
