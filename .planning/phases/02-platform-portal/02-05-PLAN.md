---
phase: 02-platform-portal
plan: 05
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - src/modules/lead-management/containers/lead-list-container.tsx
  - src/modules/lead-management/containers/lead-edit-container.tsx
  - src/modules/lead-management/hooks/use-lead-list.ts
  - src/modules/lead-management/hooks/use-lead-edit.ts
  - src/modules/lead-management/components/lead-list-ui/create-columns.tsx
  - src/modules/booking-management/containers/booking-list-container.tsx
  - src/modules/booking-management/containers/booking-create-container.tsx
  - src/modules/booking-management/containers/booking-edit-container.tsx
  - src/modules/booking-management/hooks/use-booking-list.ts
  - src/modules/booking-management/hooks/use-booking-create.ts
  - src/modules/booking-management/hooks/use-booking-edit.ts
  - src/modules/booking-management/components/booking-list-ui/create-columns.tsx
autonomous: true
requirements: [LEAD-07, LEAD-08, LEAD-09, LEAD-10, BOOK-01, BOOK-02, BOOK-03, BOOK-04, BOOK-05]
must_haves:
  truths:
    - "Lead list page renders data using data-table component with columns"
    - "Lead list pagination/sorting/filtering update URL query params correctly"
    - "Booking list page renders data using data-table component with columns"
    - "Booking list pagination/sorting/filtering update URL query params correctly"
    - "All 4 modules (categories, products, leads, bookings) use identical container + data-table pattern"
  artifacts:
    - path: "src/modules/lead-management/hooks/use-lead-list.ts"
      provides: "Lead list container hook with nuqs"
      exports: ["useLeadListContainer"]
    - path: "src/modules/lead-management/components/lead-list-ui/create-columns.tsx"
      provides: "Column definitions for lead data-table"
      exports: ["createColumns"]
    - path: "src/modules/booking-management/hooks/use-booking-list.ts"
      provides: "Booking list container hook with nuqs"
      exports: ["useBookingListContainer"]
    - path: "src/modules/booking-management/components/booking-list-ui/create-columns.tsx"
      provides: "Column definitions for booking data-table"
      exports: ["createColumns"]
  key_links:
    - from: "src/modules/lead-management/hooks/use-lead-list.ts"
      to: "src/api/leads/queries.ts"
      via: "useGetLeadList hook"
      pattern: "Hook signature matches category pattern"
    - from: "src/modules/lead-management/containers/lead-list-container.tsx"
      to: "src/components/ui/data-table"
      via: "DataTable component"
      pattern: "DataTable renders lead list with columns"
    - from: "src/modules/booking-management/hooks/use-booking-list.ts"
      to: "src/api/bookings/queries.ts"
      via: "useGetBookingList hook"
      pattern: "Hook signature matches category pattern"
    - from: "src/modules/booking-management/containers/booking-list-container.tsx"
      to: "src/components/ui/data-table"
      via: "DataTable component"
      pattern: "DataTable renders booking list with columns"
---

<objective>
Refactor leads and bookings containers/hooks using identical nuqs + TanStack Query + data-table pattern established in Plan 04 (categories/products). This ensures uniform architecture across all 4 critical admin modules before moving to forms.

Purpose: Leads and bookings are less complex than products but more complex than categories. They complete the container + data-table pattern standardization across all modules.

Output:
- Leads containers and hooks refactored with data-table rendering
- Bookings containers and hooks refactored with data-table rendering
- All 4 modules follow identical pattern (nuqs + TanStack Query + data-table)
- Forms still unchanged (refactored in Plan 07-08)
</objective>

<execution_context>
@/Users/macbookpro/Desktop/Projects/curtains/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/macbookpro/Desktop/Projects/curtains/.planning/ROADMAP.md
@/Users/macbookpro/Desktop/Projects/curtains/.planning/phases/02-platform-portal/02-CONTEXT.md
@/Users/macbookpro/Desktop/Projects/curtains/docs/requirements/refactor-phase-1.md
@/Users/macbookpro/Desktop/Projects/curtains/.planning/phases/02-platform-portal/02-04-PLAN.md
</context>

<interfaces>
From src/modules/category-management/hooks/use-category-list.ts (refactored pattern):
```typescript
export const useCategoryListContainer = () => {
  const [query, setQuery] = useQueryStates({
    page: parseAsInteger.withDefault(1),
    pageSize: parseAsInteger.withDefault(20),
    sortBy: parseAsString.withDefault('name'),
    orderBy: parseAsString.withDefault('asc'),
    search: parseAsString.withDefault(''),
  });

  const { data, isLoading, isFetching } = useGetCategoryList(query, {
    placeholderData: (prev) => prev,
  });

  return { 
    data, 
    isLoading, 
    isFetching, 
    tableData, 
    onPaginationChange, 
    onSortingChange,
    t,
  };
};
```

From src/modules/category-management/components/category-list-ui/create-columns.tsx:
```typescript
export const createColumns = ({ t }: ColumnProps): ColumnDef<ICategory>[] => [
  { accessorKey: 'id', header: ... },
  { accessorKey: 'name', header: ... },
  // ... more columns
];
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor lead list container, hook, and add data-table rendering</name>
  <files>
    - src/modules/lead-management/hooks/use-lead-list.ts
    - src/modules/lead-management/containers/lead-list-container.tsx
    - src/modules/lead-management/components/lead-list-ui/create-columns.tsx
  </files>
  <read_first>
  - src/modules/lead-management/hooks/use-lead-list.ts (current Phase 1)
  - src/modules/lead-management/containers/lead-list-container.tsx (current Phase 1)
  - src/modules/category-management/hooks/use-category-list.ts (refactored pattern from Plan 04)
  - src/modules/category-management/components/category-list-ui/create-columns.tsx (column pattern from Plan 04)
  - src/api/leads/queries.ts (Wave 1 hooks)
  </read_first>
  <action>
  Refactor leads list container/hook and add data-table columns using identical pattern from Plan 04:

  **src/modules/lead-management/hooks/use-lead-list.ts:**
  - Import: useQueryStates from 'nuqs', { useGetLeadList } from 'src/api/leads'
  - Create function useLeadListContainer() with same structure as useCategoryListContainer
  - Query state keys: page, pageSize, sortBy, orderBy, search, status (optional)
  - Defaults: page=1, pageSize=20, sortBy='createdAt', orderBy='desc', search='', status=''
  - Call useGetLeadList with query params and { placeholderData: (prev) => prev }
  - Return: data, isLoading, isFetching, tableData, pagination, handlers (onPaginationChange, onSortingChange, onSearchChange), t

  **src/modules/lead-management/containers/lead-list-container.tsx:**
  - Call useLeadListContainer() hook
  - Import DataTable, createColumns
  - Create columns: const columns = useMemo(() => createColumns({ t }), [t])
  - Render DataTable with: columns, data, pagination, loading states, handlers
  - Pattern identical to category/product list containers

  **src/modules/lead-management/components/lead-list-ui/create-columns.tsx:**
  - Create new file with column definitions for ILead
  - Columns: id, customer_name, customer_email, customer_phone, status, created_at
  - Status column: render as badge (new=blue, contacted=yellow, closed=gray)
  - Format created_at as date
  - Per Plan 04 pattern

  Pattern identical to category/product for consistency.
  </action>
  <verify>
    <automated>grep -q "useQueryStates" /Users/macbookpro/Desktop/Projects/curtains/src/modules/lead-management/hooks/use-lead-list.ts && grep -q "useGetLeadList" /Users/macbookpro/Desktop/Projects/curtains/src/modules/lead-management/hooks/use-lead-list.ts && grep -q "DataTable" /Users/macbookpro/Desktop/Projects/curtains/src/modules/lead-management/containers/lead-list-container.tsx && grep -q "createColumns" /Users/macbookpro/Desktop/Projects/curtains/src/modules/lead-management/components/lead-list-ui/create-columns.tsx</automated>
  </verify>
  <done>Lead list uses nuqs, TanStack Query, data-table component (pattern matches category/product)</done>
</task>

<task type="auto">
  <name>Task 2: Refactor lead edit containers and hooks (mutations)</name>
  <files>
    - src/modules/lead-management/hooks/use-lead-edit.ts
    - src/modules/lead-management/containers/lead-edit-container.tsx
  </files>
  <read_first>
  - src/modules/lead-management/containers/lead-edit-container.tsx (current Phase 1)
  - src/api/leads/queries.ts (useUpdateLeadMutation)
  - src/modules/category-management/hooks/use-category-edit.ts (refactored pattern)
  </read_first>
  <action>
  Refactor lead edit containers and hooks using TanStack Query mutations:

  **src/modules/lead-management/hooks/use-lead-edit.ts:**
  - Import: { useUpdateLeadMutation, useGetLeadDetail } from 'src/api/leads'
  - Create function useLeadEditForm() with same structure as useCategoryEditForm
  - Calls useUpdateLeadMutation() to handle form submission
  - Returns: isPending, onSubmit

  **src/modules/lead-management/containers/lead-edit-container.tsx:**
  - Call useLeadEditForm() hook
  - Pass isPending and onSubmit to lead edit form UI
  - Pattern identical to category/product edit

  No nuqs needed for edit (no URL state needed).
  </action>
  <verify>
    <automated>grep -q "useUpdateLeadMutation" /Users/macbookpro/Desktop/Projects/curtains/src/modules/lead-management/hooks/use-lead-edit.ts</automated>
  </verify>
  <done>Lead edit hooks use TanStack Query mutations</done>
</task>

<task type="auto">
  <name>Task 3: Refactor booking list container, hook, and add data-table rendering</name>
  <files>
    - src/modules/booking-management/hooks/use-booking-list.ts
    - src/modules/booking-management/containers/booking-list-container.tsx
    - src/modules/booking-management/components/booking-list-ui/create-columns.tsx
  </files>
  <read_first>
  - src/modules/booking-management/hooks/use-booking-list.ts (current Phase 1)
  - src/modules/booking-management/containers/booking-list-container.tsx (current Phase 1)
  - src/modules/category-management/hooks/use-category-list.ts (refactored pattern)
  - src/api/bookings/queries.ts (Wave 1 hooks)
  </read_first>
  <action>
  Refactor booking list container/hook and add data-table columns using identical pattern:

  **src/modules/booking-management/hooks/use-booking-list.ts:**
  - Import: useQueryStates from 'nuqs', { useGetBookingList } from 'src/api/bookings'
  - Create function useBookingListContainer() with same structure as useCategoryListContainer
  - Query state keys: page, pageSize, sortBy, orderBy, search, status (optional)
  - Defaults: page=1, pageSize=20, sortBy='bookingDate', orderBy='desc', search='', status=''
  - Call useGetBookingList with query params and { placeholderData: (prev) => prev }
  - Return: data, isLoading, isFetching, tableData, pagination, handlers, t

  **src/modules/booking-management/containers/booking-list-container.tsx:**
  - Call useBookingListContainer() hook
  - Import DataTable, createColumns
  - Create columns: const columns = useMemo(() => createColumns({ t }), [t])
  - Render DataTable with: columns, data, pagination, loading states, handlers
  - Pattern identical to other modules

  **src/modules/booking-management/components/booking-list-ui/create-columns.tsx:**
  - Create new file with column definitions for IBooking
  - Columns: id, lead_id, booking_date, booking_time, status, created_at
  - Status column: render as badge (pending=orange, confirmed=green, done=gray)
  - Format booking_date as date, booking_time as HH:MM
  - Per Plan 04 pattern

  Pattern identical to all other modules for consistency.
  </action>
  <verify>
    <automated>grep -q "useQueryStates" /Users/macbookpro/Desktop/Projects/curtains/src/modules/booking-management/hooks/use-booking-list.ts && grep -q "useGetBookingList" /Users/macbookpro/Desktop/Projects/curtains/src/modules/booking-management/hooks/use-booking-list.ts && grep -q "DataTable" /Users/macbookpro/Desktop/Projects/curtains/src/modules/booking-management/containers/booking-list-container.tsx && grep -q "createColumns" /Users/macbookpro/Desktop/Projects/curtains/src/modules/booking-management/components/booking-list-ui/create-columns.tsx</automated>
  </verify>
  <done>Booking list uses nuqs, TanStack Query, data-table component</done>
</task>

<task type="auto">
  <name>Task 4: Refactor booking create/edit containers and hooks</name>
  <files>
    - src/modules/booking-management/hooks/use-booking-create.ts
    - src/modules/booking-management/containers/booking-create-container.tsx
    - src/modules/booking-management/hooks/use-booking-edit.ts
    - src/modules/booking-management/containers/booking-edit-container.tsx
  </files>
  <read_first>
  - src/modules/booking-management/containers/booking-create-container.tsx (current Phase 1)
  - src/modules/booking-management/containers/booking-edit-container.tsx (current Phase 1)
  - src/api/bookings/queries.ts (useCreateBookingMutation, useUpdateBookingMutation)
  - src/modules/category-management/hooks/use-category-create.ts (refactored pattern)
  </read_first>
  <action>
  Refactor booking create/edit containers and hooks using TanStack Query mutations:

  **src/modules/booking-management/hooks/use-booking-create.ts:**
  - Import: { useCreateBookingMutation } from 'src/api/bookings'
  - Create function useBookingCreateForm()
  - Returns: isPending, onSubmit

  **src/modules/booking-management/containers/booking-create-container.tsx:**
  - Call useBookingCreateForm() hook
  - Pass to form UI

  **src/modules/booking-management/hooks/use-booking-edit.ts:**
  - Import: { useUpdateBookingMutation } from 'src/api/bookings'
  - Create function useBookingEditForm()
  - Returns: isPending, onSubmit

  **src/modules/booking-management/containers/booking-edit-container.tsx:**
  - Call useBookingEditForm() hook
  - Pass to form UI

  Pattern identical to category/product/lead for consistency.
  </action>
  <verify>
    <automated>grep -q "useCreateBookingMutation\|useUpdateBookingMutation" /Users/macbookpro/Desktop/Projects/curtains/src/modules/booking-management/hooks/use-booking-create.ts</automated>
  </verify>
  <done>Booking create/edit hooks use TanStack Query mutations</done>
</task>

<task type="auto">
  <name>Task 5: Verify TypeScript compilation for all container and data-table refactoring</name>
  <files>src/modules/lead-management/, src/modules/booking-management/</files>
  <read_first>
  - src/modules/lead-management/hooks/use-lead-list.ts (updated)
  - src/modules/lead-management/components/lead-list-ui/create-columns.tsx (new)
  - src/modules/booking-management/hooks/use-booking-list.ts (updated)
  - src/modules/booking-management/components/booking-list-ui/create-columns.tsx (new)
  - tsconfig.json
  </read_first>
  <action>
  Run TypeScript type-check to verify all container and data-table refactoring:

  ```bash
  cd /Users/macbookpro/Desktop/Projects/curtains && npx tsc --noEmit
  ```

  This verifies:
  - All 4 modules (category, product, lead, booking) containers compile together
  - nuqs imports work in all modules
  - TanStack Query hook imports from Wave 1 API modules work
  - DataTable component imports work
  - createColumns functions type-check correctly
  - Pattern consistency across all container hooks and data-table columns
  </action>
  <verify>
    <automated>cd /Users/macbookpro/Desktop/Projects/curtains && npx tsc --noEmit 2>&1 | grep -q "error" && exit 1 || exit 0</automated>
  </verify>
  <done>TypeScript compilation passes for all container and data-table refactoring</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL State → Container Hook | nuqs parses and validates URL params before passing to hooks |
| Container Hook → API | TanStack Query enforces typed query parameters |
| API Response → DataTable | TanStack Query data safely passed to table rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | Lead URL params | mitigate | useQueryStates enforces type parsing |
| T-02-16 | Tampering | Booking URL params | mitigate | useQueryStates enforces type parsing |
| T-02-21 | Injection | Lead/Booking DataTable columns | mitigate | Column cell renderers use React JSX (auto-escaped) |

</threat_model>

<verification>
After task completion:
1. ✓ Lead list hook uses useQueryStates from nuqs
2. ✓ Lead list hook calls useGetLeadList from src/api/leads
3. ✓ Lead list container renders DataTable component with create-columns
4. ✓ Lead edit hook calls useUpdateLeadMutation from src/api/leads
5. ✓ Booking list hook uses useQueryStates from nuqs
6. ✓ Booking list hook calls useGetBookingList from src/api/bookings
7. ✓ Booking list container renders DataTable component with create-columns
8. ✓ Booking create/edit hooks call create/update mutations
9. ✓ create-columns.tsx files exist for both leads and bookings
10. ✓ Container pattern identical across 4 modules (category, product, lead, booking)
11. ✓ DataTable component renders lists with pagination/sorting/filtering in all 4 modules
12. ✓ npx tsc --noEmit passes
</verification>

<success_criteria>
Phase 2 Plan 05 is complete when:
- Lead containers refactored to use nuqs + TanStack Query + data-table
- Booking containers refactored to use nuqs + TanStack Query + data-table
- All 4 critical modules follow identical container + data-table pattern
- create-columns.tsx files define columns for leads and bookings
- DataTable component renders all module lists with pagination/sorting/filtering
- TypeScript compilation passes
- Ready for Plan 06 to refactor blog, dashboard containers (with data-table)
</success_criteria>

<output>
After completion, create `.planning/phases/02-platform-portal/02-05-SUMMARY.md`
</output>
