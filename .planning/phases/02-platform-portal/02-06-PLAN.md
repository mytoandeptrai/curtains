---
phase: 02-platform-portal
plan: 06
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - src/modules/blog-management/containers/blog-list-container.tsx
  - src/modules/blog-management/containers/blog-create-container.tsx
  - src/modules/blog-management/containers/blog-edit-container.tsx
  - src/modules/blog-management/hooks/use-blog-list.ts
  - src/modules/blog-management/hooks/use-blog-create.ts
  - src/modules/blog-management/hooks/use-blog-edit.ts
  - src/modules/blog-management/components/blog-list-ui/create-columns.tsx
  - src/modules/dashboard/containers/dashboard-container.tsx
  - src/modules/dashboard/hooks/use-dashboard.ts
autonomous: true
requirements: [BLOG-01, BLOG-02, BLOG-03, BLOG-04, BLOG-05, BLOG-06, BLOG-07, STAT-01, STAT-02, STAT-03, STAT-04, STAT-05]
must_haves:
  truths:
    - "Blog list page renders data using data-table component with columns"
    - "Blog pagination/sorting/filtering update URL query params correctly"
    - "Dashboard container loads stats using TanStack Query with auto-refresh"
    - "All 6 admin modules now use consistent API integration patterns"
    - "Blog + Dashboard containers prepared for form refactoring"
  artifacts:
    - path: "src/modules/blog-management/hooks/use-blog-list.ts"
      provides: "Blog list container hook with nuqs"
      exports: ["useBlogListContainer"]
    - path: "src/modules/blog-management/components/blog-list-ui/create-columns.tsx"
      provides: "Column definitions for blog data-table"
      exports: ["createColumns"]
    - path: "src/modules/dashboard/hooks/use-dashboard.ts"
      provides: "Dashboard stats hook with TanStack Query"
      exports: ["useDashboard"]
  key_links:
    - from: "src/modules/blog-management/hooks/use-blog-list.ts"
      to: "src/api/blog/queries.ts"
      via: "useGetBlogList hook"
      pattern: "Hook signature matches category pattern"
    - from: "src/modules/blog-management/containers/blog-list-container.tsx"
      to: "src/components/ui/data-table"
      via: "DataTable component"
      pattern: "DataTable renders blog list with columns"
    - from: "src/modules/dashboard/hooks/use-dashboard.ts"
      to: "src/api/dashboard/queries.ts"
      via: "useGetDashboardStats hook"
      pattern: "Query-only pattern with auto-refetch"
---

<objective>
Complete container refactoring across all 6 admin modules by refactoring blog and dashboard. Blog follows the standard CRUD + data-table pattern, while dashboard is unique (queries only with auto-refresh, no mutations).

Purpose: After this plan, all containers use consistent TanStack Query + nuqs (list pages) or TanStack Query only (dashboard) pattern. Forms refactoring follows in Plans 07-08.

Output:
- Blog containers and hooks refactored with data-table rendering
- Dashboard container refactored to use TanStack Query stats hook with auto-refresh
- All 6 modules use identical API integration pattern
- Foundation established for form field refactoring in Plans 07-08
</objective>

<execution_context>
@/Users/macbookpro/Desktop/Projects/curtains/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/macbookpro/Desktop/Projects/curtains/.planning/ROADMAP.md
@/Users/macbookpro/Desktop/Projects/curtains/.planning/phases/02-platform-portal/02-CONTEXT.md
@/Users/macbookpro/Desktop/Projects/curtains/docs/requirements/refactor-phase-1.md
@/Users/macbookpro/Desktop/Projects/curtains/.planning/phases/02-platform-portal/02-04-PLAN.md
@/Users/macbookpro/Desktop/Projects/curtains/.planning/phases/02-platform-portal/02-05-PLAN.md
</context>

<interfaces>
From src/modules/category-management/hooks/use-category-list.ts (refactored pattern in Plan 04):
```typescript
export const useCategoryListContainer = () => {
  const [query, setQuery] = useQueryStates({...});
  const { data, isLoading, isFetching } = useGetCategoryList(query, {...});
  return { data, isLoading, isFetching, tableData, ... };
};
```

From src/modules/category-management/components/category-list-ui/create-columns.tsx (Plan 04):
```typescript
export const createColumns = ({ t }: ColumnProps): ColumnDef<ICategory>[] => [
  { accessorKey: 'id', header: ... },
  // ... more columns
];
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor blog list container, hook, and add data-table rendering</name>
  <files>
    - src/modules/blog-management/hooks/use-blog-list.ts
    - src/modules/blog-management/containers/blog-list-container.tsx
    - src/modules/blog-management/components/blog-list-ui/create-columns.tsx
  </files>
  <read_first>
  - src/modules/blog-management/hooks/use-blog-list.ts (current Phase 1)
  - src/modules/blog-management/containers/blog-list-container.tsx (current Phase 1)
  - src/modules/category-management/hooks/use-category-list.ts (refactored pattern from Plan 04)
  - src/modules/category-management/components/category-list-ui/create-columns.tsx (column pattern)
  - src/api/blog/queries.ts (Wave 1 hooks)
  </read_first>
  <action>
  Refactor blog list container/hook and add data-table columns using identical pattern from Plan 04-05:

  **src/modules/blog-management/hooks/use-blog-list.ts:**
  - Import: useQueryStates from 'nuqs', { useGetBlogList } from 'src/api/blog'
  - Create function useBlogListContainer() with same structure as useCategoryListContainer
  - Query state keys: page, pageSize, sortBy, orderBy, search, status (optional)
  - Defaults: page=1, pageSize=20, sortBy='createdAt', orderBy='desc', search='', status=''
  - Call useGetBlogList with query params and { placeholderData: (prev) => prev }
  - Return: data, isLoading, isFetching, tableData, pagination, handlers (onPaginationChange, onSortingChange, onSearchChange), t

  **src/modules/blog-management/containers/blog-list-container.tsx:**
  - Call useBlogListContainer() hook
  - Import DataTable, createColumns
  - Create columns: const columns = useMemo(() => createColumns({ t }), [t])
  - Render DataTable with: columns, data, pagination, loading states, handlers
  - Pattern identical to all previous modules

  **src/modules/blog-management/components/blog-list-ui/create-columns.tsx:**
  - Create new file with column definitions for IBlogPost
  - Columns: id, title, slug, status, created_at, updated_at
  - Status column: render as badge (draft=blue, published=green)
  - Format created_at as date
  - Per Plan 04-05 pattern

  Pattern identical to all previous modules for consistency.
  </action>
  <verify>
    <automated>grep -q "useQueryStates" /Users/macbookpro/Desktop/Projects/curtains/src/modules/blog-management/hooks/use-blog-list.ts && grep -q "useGetBlogList" /Users/macbookpro/Desktop/Projects/curtains/src/modules/blog-management/hooks/use-blog-list.ts && grep -q "DataTable" /Users/macbookpro/Desktop/Projects/curtains/src/modules/blog-management/containers/blog-list-container.tsx && grep -q "createColumns" /Users/macbookpro/Desktop/Projects/curtains/src/modules/blog-management/components/blog-list-ui/create-columns.tsx</automated>
  </verify>
  <done>Blog list uses nuqs, TanStack Query, data-table component (pattern matches all modules)</done>
</task>

<task type="auto">
  <name>Task 2: Refactor blog create/edit containers and hooks</name>
  <files>
    - src/modules/blog-management/hooks/use-blog-create.ts
    - src/modules/blog-management/containers/blog-create-container.tsx
    - src/modules/blog-management/hooks/use-blog-edit.ts
    - src/modules/blog-management/containers/blog-edit-container.tsx
  </files>
  <read_first>
  - src/modules/blog-management/containers/blog-create-container.tsx (current Phase 1)
  - src/modules/blog-management/containers/blog-edit-container.tsx (current Phase 1)
  - src/api/blog/queries.ts (useCreateBlogMutation, useUpdateBlogMutation)
  - src/modules/category-management/hooks/use-category-create.ts (refactored pattern)
  </read_first>
  <action>
  Refactor blog create/edit containers and hooks to use TanStack Query mutations:

  **src/modules/blog-management/hooks/use-blog-create.ts:**
  - Import: { useCreateBlogMutation } from 'src/api/blog'
  - Create function useBlogCreateForm()
  - Calls useCreateBlogMutation() to handle form submission
  - Returns: isPending, onSubmit

  **src/modules/blog-management/containers/blog-create-container.tsx:**
  - Call useBlogCreateForm() hook
  - Pass isPending and onSubmit to form UI

  **src/modules/blog-management/hooks/use-blog-edit.ts:**
  - Import: { useUpdateBlogMutation } from 'src/api/blog'
  - Create function useBlogEditForm()
  - Returns: isPending, onSubmit

  **src/modules/blog-management/containers/blog-edit-container.tsx:**
  - Call useBlogEditForm() hook
  - Pass isPending and onSubmit to form UI

  Pattern identical to all other modules for consistency.
  </action>
  <verify>
    <automated>grep -q "useCreateBlogMutation\|useUpdateBlogMutation" /Users/macbookpro/Desktop/Projects/curtains/src/modules/blog-management/hooks/use-blog-create.ts</automated>
  </verify>
  <done>Blog create/edit hooks use TanStack Query mutations</done>
</task>

<task type="auto">
  <name>Task 3: Refactor dashboard container and hook (stats queries only)</name>
  <files>
    - src/modules/dashboard/hooks/use-dashboard.ts
    - src/modules/dashboard/containers/dashboard-container.tsx
  </files>
  <read_first>
  - src/modules/dashboard/containers/dashboard-container.tsx (current Phase 1)
  - src/modules/dashboard/ (to understand stats displayed)
  - src/api/dashboard/queries.ts (Wave 1 hook: useGetDashboardStats)
  </read_first>
  <action>
  Refactor dashboard container/hook to use TanStack Query for stats (no URL state needed, no mutations):

  **src/modules/dashboard/hooks/use-dashboard.ts:**
  - Import: { useGetDashboardStats } from 'src/api/dashboard'
  - Create function useDashboard() that:
    1. Calls useGetDashboardStats() hook with options: { refetchInterval: 60000 } (refresh stats every 60s)
    2. Returns: data (IDashboardStats), isLoading, isFetching, error
  - No nuqs needed (dashboard stats don't change based on URL params)
  - No mutations needed (dashboard is read-only)

  **src/modules/dashboard/containers/dashboard-container.tsx:**
  - Call useDashboard() hook
  - Pass data, isLoading, isFetching to dashboard UI component
  - Dashboard displays: totalLeads, totalLeadsThisWeek, conversionRate, confirmedBookings, popularProducts, leadsTrend
  - Show loading skeleton while isFetching

  Simple pattern: No pagination, no filters, just stats query with auto-refetch every 60 seconds.
  </action>
  <verify>
    <automated>grep -q "useGetDashboardStats" /Users/macbookpro/Desktop/Projects/curtains/src/modules/dashboard/hooks/use-dashboard.ts && grep -q "refetchInterval" /Users/macbookpro/Desktop/Projects/curtains/src/modules/dashboard/hooks/use-dashboard.ts</automated>
  </verify>
  <done>Dashboard container refactored to use TanStack Query stats hook with auto-refresh</done>
</task>

<task type="auto">
  <name>Task 4: Verify TypeScript compilation and container refactoring complete</name>
  <files>src/modules/blog-management/, src/modules/dashboard/</files>
  <read_first>
  - src/modules/blog-management/hooks/use-blog-list.ts (updated)
  - src/modules/blog-management/components/blog-list-ui/create-columns.tsx (new)
  - src/modules/dashboard/hooks/use-dashboard.ts (updated)
  - tsconfig.json
  </read_first>
  <action>
  Run TypeScript type-check to verify all 6 modules' container refactoring is complete:

  ```bash
  cd /Users/macbookpro/Desktop/Projects/curtains && npx tsc --noEmit
  ```

  This verifies:
  - All 6 modules (category, product, lead, booking, blog, dashboard) containers compile together
  - Blog nuqs imports and TanStack Query hooks work
  - Blog DataTable component and createColumns type-check
  - Dashboard TanStack Query hook works with refetchInterval option
  - Complete Wave 1+2 API integration is TypeScript-clean
  </action>
  <verify>
    <automated>cd /Users/macbookpro/Desktop/Projects/curtains && npx tsc --noEmit 2>&1 | grep -q "error" && exit 1 || exit 0</automated>
  </verify>
  <done>TypeScript compilation passes for all 6 modules (container refactoring complete)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blog List URL State | nuqs manages pagination/filter params (no sensitive data) |
| Dashboard Stats API | useGetDashboardStats queries aggregated admin stats (no PII) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Information Disclosure | Dashboard stats | accept | Stats are aggregated counts only; no PII exposed in statistics |
| T-02-22 | Injection | Blog DataTable column rendering | mitigate | Column cell renderers use React JSX (auto-escaped), no raw HTML injection |

</threat_model>

<verification>
After task completion:
1. ✓ Blog list hook uses useQueryStates from nuqs
2. ✓ Blog list hook calls useGetBlogList from src/api/blog
3. ✓ Blog list container renders DataTable component with create-columns
4. ✓ Blog create/edit hooks call mutations from src/api/blog
5. ✓ Dashboard hook calls useGetDashboardStats from src/api/dashboard
6. ✓ Dashboard hook sets refetchInterval for auto-refresh (60 seconds)
7. ✓ All 6 modules follow consistent pattern (5 with nuqs+TanStack Query, 1 with TanStack Query only)
8. ✓ All 5 list-based modules (categories, products, leads, bookings, blog) use data-table
9. ✓ npx tsc --noEmit passes
</verification>

<success_criteria>
Phase 2 Plan 06 is complete when:
- Blog containers refactored to use nuqs + TanStack Query + data-table
- Blog create-columns.tsx defines columns for blog list rendering
- Dashboard container refactored to use TanStack Query stats hook with auto-refresh
- All 6 admin modules use consistent API integration
- TypeScript compilation passes
- Container layer refactoring complete across all modules (Plans 04-06)
- Ready for form field refactoring in Plans 07-08
</success_criteria>

<output>
After completion, create `.planning/phases/02-platform-portal/02-06-SUMMARY.md`
</output>
