---
phase: 02-platform-portal
plan: 07
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06]
files_modified:
  - src/modules/category-management/components/category-create-ui/category-create-form.tsx
  - src/modules/category-management/components/category-edit-ui/category-edit-form.tsx
  - src/modules/product-management/components/product-create-ui/product-create-form.tsx
  - src/modules/product-management/components/product-edit-ui/product-edit-form.tsx
autonomous: true
requirements: [CAT-01, CAT-02, PROD-01, PROD-02, PROD-03, PROD-05]
must_haves:
  truths:
    - "Category create/edit forms include complete field coverage (name, slug, description, SEO)"
    - "Product create/edit forms include complete field coverage with rich text editor"
    - "Product variant fields (color, material, finish, layers) fully configured in form"
    - "Product image upload with preview working in forms"
    - "All forms save all schema fields to database without truncation"
  artifacts:
    - path: "src/modules/category-management/components/category-create-ui/category-create-form.tsx"
      provides: "Complete category form with all schema fields"
      contains: "name, slug, description, seoTitle, seoDescription (5+ fields)"
    - path: "src/modules/product-management/components/product-create-ui/product-create-form.tsx"
      provides: "Complete product form with rich text editor and variants"
      contains: "All product schema fields including variants array, image upload, rich description, SEO"
  key_links:
    - from: "src/modules/product-management/components/product-create-ui/product-create-form.tsx"
      to: "form-minimal-tiptap"
      via: "Rich text editor import"
      pattern: "import FormRichText from form-minimal-tiptap"
---

<objective>
Refactor category and product form components to include complete field coverage. Categories: basic + SEO. Products: all fields including variants (color, material, finish, layers), images, pricing, and rich text description.

Purpose: Forms with minimal fields (3-4) are not production-ready. Complete forms allow admins to manage all product/category attributes without workarounds.

Output:
- Category forms: Complete with name, slug, description, SEO fields
- Product forms: Complete with variants, images, pricing, SEO, and rich text description
- All forms save all schema fields correctly
</objective>

<execution_context>
@/Users/macbookpro/Desktop/Projects/curtains/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/macbookpro/Desktop/Projects/curtains/.planning/ROADMAP.md
@/Users/macbookpro/Desktop/Projects/curtains/.planning/phases/02-platform-portal/02-CONTEXT.md
@/Users/macbookpro/Desktop/Projects/curtains/docs/requirements/refactor-phase-1.md
</context>

<interfaces>
From Phase 1 schema definitions:

Category schema:
```typescript
export interface Category {
  id: string;
  name: string;
  slug: string;
  description: string;
  seo_title: string;
  seo_description: string;
  created_at: string;
}
```

Product schema (from refactor-phase-1.md):
```typescript
export const productFormSchema = z.object({
  name: z.string().min(3),
  description: z.string().min(20),
  price: z.number().min(1),
  salePrice: z.number().optional(),
  sku: z.string(),
  categoryId: z.string(),
  stockQuantity: z.number(),
  unit: z.string(),
  material: z.string(),
  finish: z.string(),
  color: z.string(),
  variants: z.array(z.object({
    color: z.string(),
    material: z.string(),
    finish: z.string(),
    layers: z.array(z.string()).optional(),
  })).optional(),
  slug: z.string(),
  metaTitle: z.string(),
  metaDescription: z.string(),
  isFeatured: z.boolean(),
  imageUrl: z.array(z.custom<IMedia>()),
  images: z.array(z.custom<IMedia>()).optional(),
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor category create/edit forms with complete fields</name>
  <files>
    - src/modules/category-management/components/category-create-ui/category-create-form.tsx
    - src/modules/category-management/components/category-edit-ui/category-edit-form.tsx
  </files>
  <read_first>
  - src/modules/category-management/components/category-create-ui/category-create-form.tsx (current Phase 1)
  - src/modules/category-management/components/category-edit-ui/category-edit-form.tsx (current Phase 1)
  - src/types/database.ts (Category type definition)
  - docs/requirements/refactor-phase-1.md section D (complete field coverage pattern)
  </read_first>
  <action>
  Update category forms to include all schema fields:

  **Required fields in both create and edit forms:**
  1. name (text input, required, min 3 chars)
  2. slug (text input, required, with validation pattern for lowercase-with-hyphens)
  3. description (textarea or form-minimal-tiptap, optional)
  4. seo_title (text input for SEO, optional, min 10 chars)
  5. seo_description (textarea for SEO, optional, min 20 chars)

  **Form structure:**
  - Group 1: Basic Information (name, slug)
  - Group 2: Description (textarea)
  - Group 3: SEO (seo_title, seo_description)

  Use existing form components (TextField, TextAreaField) from src/components/form-field/.

  For edit form, pre-fill all fields from existing category data.

  Acceptance: Category form renders all 5 fields, form submission saves all fields to database without data loss.
  </action>
  <verify>
    <automated>grep -c "name\|slug\|description\|seo_title\|seo_description" /Users/macbookpro/Desktop/Projects/curtains/src/modules/category-management/components/category-create-ui/category-create-form.tsx</automated>
  </verify>
  <done>Category forms include complete field coverage (5 fields)</done>
</task>

<task type="auto">
  <name>Task 2: Refactor product create/edit forms with complete fields, variants, and rich text</name>
  <files>
    - src/modules/product-management/components/product-create-ui/product-create-form.tsx
    - src/modules/product-management/components/product-edit-ui/product-edit-form.tsx
  </files>
  <read_first>
  - src/modules/product-management/components/product-create-ui/product-create-form.tsx (current Phase 1)
  - src/modules/product-management/components/product-edit-ui/product-edit-form.tsx (current Phase 1)
  - docs/requirements/refactor-phase-1.md section C (product form complete field coverage with variants)
  - docs/requirements/refactor-phase-1.md section D (rich text editor usage)
  - src/components/form-field/ (available form field components)
  </read_first>
  <action>
  Update product forms to include complete field coverage including variants and rich text editor:

  **Basic Information Section:**
  1. name (text input, required, min 3 chars)
  2. sku (text input, required, min 3 chars)
  3. slug (text input, required, regex: lowercase-with-hyphens)

  **Pricing & Inventory Section:**
  4. price (number input, required, min 1)
  5. salePrice (number input, optional)
  6. stockQuantity (number input, required, min 1)
  7. unit (text input, required, e.g., pcs, kg, m)

  **Product Details Section:**
  8. categoryId (select dropdown, required)
  9. description (FormRichText using form-minimal-tiptap, required, min 20 chars) — CRITICAL: Replace textarea with FormRichText
  10. featured (boolean switch/toggle, optional)

  **Product Variants Section** (CRITICAL - was missing in original):
  Variants array with add/remove buttons. Each variant has:
  - 11. color (text input, required, e.g., red, blue)
  - 12. material (text input, required, e.g., cotton, polyester)
  - 13. finish (text input, required, e.g., matte, glossy)
  - 14. layers (array of text inputs, optional, e.g., layer 1, layer 2)

  **Images Section:**
  15. imageUrl (file dropzone for primary image, required, single file upload)
  16. images (file dropzone for additional images, optional, max 4 files)

  **SEO Section:**
  17. metaTitle (text input for SEO, optional, min 10 chars)
  18. metaDescription (textarea for SEO, optional, min 20 chars)

  **Form Structure:**
  - Group 1: Basic Information (name, sku, slug)
  - Group 2: Pricing & Inventory (price, salePrice, stockQuantity, unit)
  - Group 3: Product Details (categoryId, description [rich text], featured)
  - Group 4: Product Variants (color, material, finish, layers — with add/remove variant buttons)
  - Group 5: Images (primary image, additional images with preview)
  - Group 6: SEO (metaTitle, metaDescription)

  **Key changes from Phase 1:**
  - Description field MUST use FormRichText (not textarea) for markdown/rich text editing
  - Variants must be a full array with add/remove buttons (not optional text fields)
  - All fields must be saved and persisted (no truncation)

  For edit form, pre-fill all fields including variants array from existing product data.

  Acceptance: Product form renders all 18 fields plus variant array management. Form submission saves all fields to database.
  </action>
  <verify>
    <automated>grep -q "FormRichText\|form-minimal-tiptap" /Users/macbookpro/Desktop/Projects/curtains/src/modules/product-management/components/product-create-ui/product-create-form.tsx && grep -q "variants" /Users/macbookpro/Desktop/Projects/curtains/src/modules/product-management/components/product-create-ui/product-create-form.tsx && grep -c "TextField\|SelectField\|TextAreaField" /Users/macbookpro/Desktop/Projects/curtains/src/modules/product-management/components/product-create-ui/product-create-form.tsx</automated>
  </verify>
  <done>Product forms include 18 fields with FormRichText for description and full variants array support</done>
</task>

<task type="auto">
  <name>Task 3: Verify TypeScript compilation and form refactoring</name>
  <files>src/modules/category-management/, src/modules/product-management/</files>
  <read_first>
  - All refactored form files
  - src/components/form-field/ (form components used)
  - tsconfig.json
  </read_first>
  <action>
  Run TypeScript type-check to verify form refactoring:

  ```bash
  cd /Users/macbookpro/Desktop/Projects/curtains && npx tsc --noEmit
  ```

  This verifies:
  - All form field imports resolve (TextField, SelectField, TextAreaField, FormRichText)
  - Category and product forms compile without errors
  - Rich text editor imports work
  - Variants array form handling type-checks
  - Complete Phase 2 plans 07 refactoring is TypeScript-clean
  </action>
  <verify>
    <automated>cd /Users/macbookpro/Desktop/Projects/curtains && npx tsc --noEmit 2>&1 | grep -q "error" && exit 1 || exit 0</automated>
  </verify>
  <done>TypeScript compilation passes for category and product form refactoring</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input → Form Validation | Zod schema validates all form inputs before submission |
| Rich Text Editor → Content | form-minimal-tiptap sanitizes markdown/rich text content |
| Form → Database | API sends validated form data to backend RLS-protected endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Injection | Product description field | mitigate | form-minimal-tiptap sanitizes rich text; backend validates input |
| T-02-19 | Injection | All form fields | mitigate | Zod schema validation on client; backend RLS policies enforce constraints |
| T-02-23 | Tampering | Variants array | mitigate | Form validation ensures variant objects match schema; backend RLS validates |

</threat_model>

<verification>
After task completion:
1. ✓ Category form includes 5 fields (name, slug, description, seo_title, seo_description)
2. ✓ Category form saves all fields to database
3. ✓ Product form includes 18 fields (basic, pricing, details, variants, images, SEO)
4. ✓ Product description uses FormRichText (not textarea)
5. ✓ Product variants array with add/remove button support
6. ✓ Product image upload fields present (primary + additional)
7. ✓ All SEO fields present in both forms
8. ✓ Forms compile without TypeScript errors
9. ✓ npx tsc --noEmit passes
</verification>

<success_criteria>
Phase 2 Plan 07 is complete when:
- Category forms include complete field coverage (5 fields)
- Product forms include complete field coverage (18 fields including variants)
- Product description uses form-minimal-tiptap rich text editor
- Product variants array fully configurable with add/remove buttons
- All forms render without errors and save all fields to database
- TypeScript compilation passes
- Ready for Plan 08 (lead, booking, blog, dashboard forms + TypeScript verification)
</success_criteria>

<output>
After completion, create `.planning/phases/02-platform-portal/02-07-SUMMARY.md`
</output>
