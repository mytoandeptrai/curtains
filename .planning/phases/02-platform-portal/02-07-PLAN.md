---
phase: 02-platform-portal
plan: 07
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06]
files_modified:
  - src/modules/category-management/components/category-create-ui/category-create-form.tsx
  - src/modules/category-management/components/category-edit-ui/category-edit-form.tsx
  - src/modules/product-management/components/product-create-ui/product-create-form.tsx
  - src/modules/product-management/components/product-edit-ui/product-edit-form.tsx
  - src/modules/lead-management/components/lead-edit-ui/lead-edit-form.tsx
  - src/modules/booking-management/components/booking-create-ui/booking-create-form.tsx
  - src/modules/booking-management/components/booking-edit-ui/booking-edit-form.tsx
  - src/modules/blog-management/components/blog-create-ui/blog-create-form.tsx
  - src/modules/blog-management/components/blog-edit-ui/blog-edit-form.tsx
autonomous: true
requirements: [CAT-01, CAT-02, PROD-01, PROD-02, PROD-03, PROD-05, LEAD-08, BOOK-01, BOOK-02, BLOG-01, BLOG-02, BLOG-03, BLOG-04, BLOG-05]
must_haves:
  truths:
    - "All form components include complete field coverage (not just 3-4 fields)"
    - "Rich text fields (product/blog descriptions) use form-minimal-tiptap instead of textarea"
    - "SEO fields included where applicable (products, blog, categories)"
    - "Forms render without errors and save all fields to database"
  artifacts:
    - path: "src/modules/category-management/components/category-create-ui/category-create-form.tsx"
      provides: "Complete category form with all schema fields"
      contains: "name, slug, description, seoTitle, seoDescription"
    - path: "src/modules/product-management/components/product-create-ui/product-create-form.tsx"
      provides: "Complete product form with rich text editor"
      contains: "All product schema fields including image upload, rich description, SEO"
    - path: "src/modules/blog-management/components/blog-create-ui/blog-create-form.tsx"
      provides: "Complete blog form with rich text editor"
      contains: "title, slug, content (rich text), status, seoTitle, seoDescription"
  key_links:
    - from: "src/modules/product-management/components/product-create-ui/product-create-form.tsx"
      to: "form-minimal-tiptap"
      via: "Rich text editor import"
      pattern: "import FormRichText from form-minimal-tiptap"
    - from: "src/modules/blog-management/components/blog-create-ui/blog-create-form.tsx"
      to: "form-minimal-tiptap"
      via: "Rich text editor import"
      pattern: "import FormRichText from form-minimal-tiptap"
---

<objective>
Refactor all form components to include complete field coverage per schema. This includes adding missing fields, replacing textareas with form-minimal-tiptap for rich text, and ensuring SEO fields are present where applicable.

Purpose: Forms with minimal fields (3-4) are not production-ready. Complete forms allow admin to manage all product/blog attributes without workarounds.

Output:
- Category forms: Complete name, slug, description, SEO fields
- Product forms: Complete with variants, images, pricing, SEO, rich text description
- Lead/booking forms: Complete with all editable fields
- Blog forms: Complete with rich text content editor, SEO fields
- All forms save all schema fields to database
</objective>

<execution_context>
@/Users/macbookpro/Desktop/Projects/curtains/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/macbookpro/Desktop/Projects/curtains/.planning/phases/02-platform-portal/02-CONTEXT.md
@/Users/macbookpro/Desktop/Projects/curtains/docs/requirements/refactor-phase-1.md
@/Users/macbookpro/Desktop/Projects/curtains/.planning/phases/02-platform-portal/02-CONTEXT.md
</context>

<interfaces>
From Phase 1 schema definitions extracted from use-*-list.ts files:

Category schema:
```typescript
export interface Category {
  id: string;
  name: string;
  slug: string;
  description: string;
  seo_title: string;
  seo_description: string;
  created_at: string;
}
```

Product schema (from refactor-phase-1.md example):
```typescript
export const productFormSchema = z.object({
  name: z.string().min(3),
  description: z.string().min(20),
  price: z.number().min(1),
  salePrice: z.number().optional(),
  sku: z.string(),
  categoryId: z.string(),
  stockQuantity: z.number(),
  unit: z.string(),
  material: z.string(),
  finish: z.string(),
  color: z.string(),
  slug: z.string(),
  metaTitle: z.string(),
  metaDescription: z.string(),
  isFeatured: z.boolean(),
  imageUrl: z.array(z.custom<IMedia>()),
  images: z.array(z.custom<IMedia>()).optional(),
});
```

Blog schema:
```typescript
export interface BlogPost {
  id: string;
  title: string;
  slug: string;
  content: string;
  status: 'draft' | 'published';
  featured_image: string;
  seo_title: string;
  seo_description: string;
  created_at: string;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor category create/edit forms with complete fields</name>
  <files>
    - src/modules/category-management/components/category-create-ui/category-create-form.tsx
    - src/modules/category-management/components/category-edit-ui/category-edit-form.tsx
  </files>
  <read_first>
  - src/modules/category-management/components/category-create-ui/category-create-form.tsx (current Phase 1)
  - src/modules/category-management/components/category-edit-ui/category-edit-form.tsx (current Phase 1)
  - src/types/database.ts (Category type definition)
  - docs/requirements/refactor-phase-1.md section D (complete field coverage pattern)
  </read_first>
  <action>
  Update category forms to include all schema fields:

  **Required fields in both create and edit forms:**
  1. name (text input, required)
  2. slug (text input, required, with validation pattern)
  3. description (textarea or form-minimal-tiptap, optional)
  4. seo_title (text input for SEO, optional)
  5. seo_description (textarea for SEO, optional)

  **Form structure:**
  - Group 1: Basic Information (name, slug)
  - Group 2: Description (use textarea or rich text field)
  - Group 3: SEO (seo_title, seo_description)

  Use existing form components (TextField, TextAreaField, FormRichText) from src/components/form-field/.

  For edit form, pre-fill all fields from existing category data.
  </action>
  <verify>
    <automated>grep -c "name\|slug\|description\|seo_title\|seo_description" /Users/macbookpro/Desktop/Projects/curtains/src/modules/category-management/components/category-create-ui/category-create-form.tsx</automated>
  </verify>
  <done>Category forms include complete field coverage (5+ fields)</done>
</task>

<task type="auto">
  <name>Task 2: Refactor product create/edit forms with complete fields and rich text</name>
  <files>
    - src/modules/product-management/components/product-create-ui/product-create-form.tsx
    - src/modules/product-management/components/product-edit-ui/product-edit-form.tsx
  </files>
  <read_first>
  - src/modules/product-management/components/product-create-ui/product-create-form.tsx (current Phase 1)
  - src/modules/product-management/components/product-edit-ui/product-edit-form.tsx (current Phase 1)
  - docs/requirements/refactor-phase-1.md section C (product form complete field coverage example)
  - docs/requirements/refactor-phase-1.md section D (rich text editor usage)
  - src/components/form-field/ (available form field components)
  </read_first>
  <action>
  Update product forms to include complete field coverage with rich text editor:

  **Required fields in both create and edit forms:**
  Basic Information:
  1. name (text input, required)
  2. sku (text input, required)
  3. slug (text input, required)

  Pricing & Inventory:
  4. price (number input, required, min 1)
  5. salePrice (number input, optional)
  6. stockQuantity (number input, required)
  7. unit (text input, required, e.g., pcs, kg, m)

  Product Details:
  8. categoryId (select dropdown, required)
  9. description (FormRichText using form-minimal-tiptap, required, replaces textarea)
  10. featured (boolean switch/toggle, optional)

  Physical Properties:
  11. material (text input, optional)
  12. finish (text input, optional)
  13. color (text input, optional)

  Images:
  14. imageUrl (file dropzone for primary image, required)
  15. images (file dropzone for additional images, optional, max 4)

  SEO:
  16. metaTitle (text input for SEO, optional)
  17. metaDescription (textarea for SEO, optional)

  **Key change:** Replace textarea for description with FormRichText:
  ```typescript
  <FormRichText
    control={control}
    name="description"
    label="Description"
    placeholder="Enter product description"
    required
    disabled={isPending}
  />
  ```

  For edit form, pre-fill all fields from existing product data.
  </action>
  <verify>
    <automated>grep -q "FormRichText\|form-minimal-tiptap" /Users/macbookpro/Desktop/Projects/curtains/src/modules/product-management/components/product-create-ui/product-create-form.tsx && grep -c "TextField\|SelectField" /Users/macbookpro/Desktop/Projects/curtains/src/modules/product-management/components/product-create-ui/product-create-form.tsx</automated>
  </verify>
  <done>Product forms include 17 fields with form-minimal-tiptap for description</done>
</task>

<task type="auto">
  <name>Task 3: Refactor lead edit form with complete fields</name>
  <files>
    - src/modules/lead-management/components/lead-edit-ui/lead-edit-form.tsx
  </files>
  <read_first>
  - src/modules/lead-management/components/lead-edit-ui/lead-edit-form.tsx (current Phase 1)
  - src/types/database.ts (Lead type definition)
  - Lead schema: customer_name, customer_email, customer_phone, status, notes
  </read_first>
  <action>
  Update lead edit form to include all editable fields:

  **Required fields in lead edit form:**
  1. customer_name (text input, required)
  2. customer_email (email input, required)
  3. customer_phone (phone input, required)
  4. status (select dropdown: 'new' | 'contacted' | 'closed', required)
  5. notes (textarea, optional, for admin notes about lead)

  Form structure:
  - Group 1: Customer Info (name, email, phone)
  - Group 2: Lead Management (status, notes)

  Pre-fill all fields from existing lead data.
  </action>
  <verify>
    <automated>grep -c "customer_name\|customer_email\|customer_phone\|status\|notes" /Users/macbookpro/Desktop/Projects/curtains/src/modules/lead-management/components/lead-edit-ui/lead-edit-form.tsx</automated>
  </verify>
  <done>Lead form includes all 5 editable fields</done>
</task>

<task type="auto">
  <name>Task 4: Refactor booking create/edit forms with complete fields</name>
  <files>
    - src/modules/booking-management/components/booking-create-ui/booking-create-form.tsx
    - src/modules/booking-management/components/booking-edit-ui/booking-edit-form.tsx
  </files>
  <read_first>
  - src/modules/booking-management/components/booking-create-ui/booking-create-form.tsx (current Phase 1)
  - src/modules/booking-management/components/booking-edit-ui/booking-edit-form.tsx (current Phase 1)
  - Booking schema: lead_id, booking_date, booking_time, status, notes
  </read_first>
  <action>
  Update booking forms to include all fields:

  **Required fields in booking create/edit forms:**
  1. lead_id (select dropdown from leads, required)
  2. booking_date (date picker, required, format YYYY-MM-DD)
  3. booking_time (time picker or text input HH:MM, required)
  4. status (select dropdown: 'pending' | 'confirmed' | 'done', required)
  5. notes (textarea, optional, for booking notes)

  Form structure:
  - Group 1: Booking Details (lead_id, booking_date, booking_time)
  - Group 2: Status & Notes (status, notes)

  For edit form, pre-fill all fields from existing booking data.
  </action>
  <verify>
    <automated>grep -c "lead_id\|booking_date\|booking_time\|status\|notes" /Users/macbookpro/Desktop/Projects/curtains/src/modules/booking-management/components/booking-create-ui/booking-create-form.tsx</automated>
  </verify>
  <done>Booking forms include all 5 fields</done>
</task>

<task type="auto">
  <name>Task 5: Refactor blog create/edit forms with rich text editor and SEO</name>
  <files>
    - src/modules/blog-management/components/blog-create-ui/blog-create-form.tsx
    - src/modules/blog-management/components/blog-edit-ui/blog-edit-form.tsx
  </files>
  <read_first>
  - src/modules/blog-management/components/blog-create-ui/blog-create-form.tsx (current Phase 1)
  - src/modules/blog-management/components/blog-edit-ui/blog-edit-form.tsx (current Phase 1)
  - docs/requirements/refactor-phase-1.md section D (form-minimal-tiptap pattern)
  - Blog schema: title, slug, content, status, featured_image, seo_title, seo_description
  </read_first>
  <action>
  Update blog forms to include complete fields with rich text editor:

  **Required fields in blog create/edit forms:**
  Basic Information:
  1. title (text input, required)
  2. slug (text input, required, with validation)
  3. status (select dropdown: 'draft' | 'published', required)

  Content:
  4. content (FormRichText using form-minimal-tiptap, required, replaces textarea)
  5. featured_image (file dropzone for thumbnail image, optional)

  SEO:
  6. seo_title (text input for SEO, optional)
  7. seo_description (textarea for SEO, optional)

  **Key change:** Use FormRichText for blog content:
  ```typescript
  <FormRichText
    control={control}
    name="content"
    label="Content"
    placeholder="Enter blog post content"
    required
    disabled={isPending}
  />
  ```

  Form structure:
  - Group 1: Basic (title, slug, status)
  - Group 2: Content (content with rich text editor)
  - Group 3: Featured Image (image upload)
  - Group 4: SEO (seo_title, seo_description)

  For edit form, pre-fill all fields from existing blog data.
  </action>
  <verify>
    <automated>grep -q "FormRichText\|form-minimal-tiptap" /Users/macbookpro/Desktop/Projects/curtains/src/modules/blog-management/components/blog-create-ui/blog-create-form.tsx && grep -c "title\|slug\|content\|status\|featured_image\|seo_title\|seo_description" /Users/macbookpro/Desktop/Projects/curtains/src/modules/blog-management/components/blog-create-ui/blog-create-form.tsx</automated>
  </verify>
  <done>Blog forms include 7 fields with form-minimal-tiptap for content</done>
</task>

<task type="auto">
  <name>Task 6: Verify TypeScript compilation and form refactoring complete</name>
  <files>src/modules/</files>
  <read_first>
  - All refactored form files
  - src/components/form-field/ (form components used)
  - tsconfig.json
  </read_first>
  <action>
  Run TypeScript type-check to verify all form refactoring:

  ```bash
  cd /Users/macbookpro/Desktop/Projects/curtains && npx tsc --noEmit
  ```

  This verifies:
  - All form field imports resolve (TextField, SelectField, TextAreaField, FormRichText)
  - All forms compile without errors
  - Rich text editor imports work
  - Complete Phase 2 refactoring is TypeScript-clean
  </action>
  <verify>
    <automated>cd /Users/macbookpro/Desktop/Projects/curtains && npx tsc --noEmit 2>&1 | grep -q "error" && exit 1 || exit 0</automated>
  </verify>
  <done>TypeScript compilation passes for all form refactoring</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input → Form Validation | Zod schema validates all form inputs before submission |
| Rich Text Editor → Content | form-minimal-tiptap sanitizes markdown/rich text content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Injection | Product/Blog description fields | mitigate | form-minimal-tiptap sanitizes rich text; backend validates input |
| T-02-19 | Injection | All form fields | mitigate | Zod schema validation on client; backend RLS policies enforce constraints |

</threat_model>

<verification>
After task completion:
1. ✓ Category forms include 5+ fields (name, slug, description, seo_title, seo_description)
2. ✓ Product forms include 17 fields with FormRichText for description
3. ✓ Lead form includes 5 editable fields
4. ✓ Booking forms include 5 fields
5. ✓ Blog forms include 7 fields with FormRichText for content
6. ✓ All description/content fields use FormRichText (not textarea)
7. ✓ All SEO fields present in product/blog forms
8. ✓ npx tsc --noEmit passes
</verification>

<success_criteria>
Phase 2 Plan 07 is complete when:
- All form components include complete field coverage
- Rich text editor (form-minimal-tiptap) used for description/content fields
- SEO fields present in product, blog, category forms
- All forms render without errors and save all fields
- TypeScript compilation passes
- Phase 2 refactoring complete: API modules + containers + forms all follow new patterns
</success_criteria>

<output>
After completion, create `.planning/phases/02-platform-portal/02-07-SUMMARY.md`
</output>
