---
phase: 02-platform-portal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/blog/index.ts
  - src/api/blog/keys.ts
  - src/api/blog/types.ts
  - src/api/blog/requests.ts
  - src/api/blog/queries.ts
  - src/api/dashboard/index.ts
  - src/api/dashboard/keys.ts
  - src/api/dashboard/types.ts
  - src/api/dashboard/requests.ts
  - src/api/dashboard/queries.ts
autonomous: true
requirements: [BLOG-01, BLOG-02, BLOG-03, BLOG-04, BLOG-05, BLOG-06, BLOG-07, STAT-01, STAT-02, STAT-03, STAT-04, STAT-05]
must_haves:
  truths:
    - "Blog API module created using category pattern (TanStack Query + HTTP instance)"
    - "Dashboard stats API module created using category pattern"
    - "All modules in Wave 1 now complete with consistent pattern"
    - "Foundation ready for Wave 2 container and form refactoring"
  artifacts:
    - path: "src/api/blog/queries.ts"
      provides: "Blog query/mutation hooks"
      exports: ["useGetBlogList", "useGetBlogDetail", "useCreateBlogMutation", "useUpdateBlogMutation", "useDeleteBlogMutation"]
    - path: "src/api/dashboard/queries.ts"
      provides: "Dashboard stats query hooks"
      exports: ["useGetDashboardStats"]
  key_links:
    - from: "src/api/blog/queries.ts"
      to: "src/api/categories/queries.ts"
      via: "pattern replication"
      pattern: "Hook signature structure matches category pattern"
    - from: "src/api/dashboard/queries.ts"
      to: "src/api/categories/queries.ts"
      via: "pattern replication"
      pattern: "Query hook structure for stats"
---

<objective>
Complete Wave 1 by creating blog and dashboard API modules. These are the final two modules that will establish the complete TanStack Query pattern foundation for all admin modules.

Purpose: Blog is similar to products (has content fields), dashboard is stats-only (no mutations, just queries). Together, they complete the API layer refactoring before Wave 2 tackles containers and forms.

Output:
- src/api/blog/ complete module (TanStack Query + HTTP instance)
- src/api/dashboard/ complete module (TanStack Query for stats queries only)
- All Wave 1 foundation complete and ready for Wave 2 execution
</objective>

<execution_context>
@/Users/macbookpro/Desktop/Projects/curtains/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/macbookpro/Desktop/Projects/curtains/.planning/phases/02-platform-portal/02-CONTEXT.md
@/Users/macbookpro/Desktop/Projects/curtains/docs/requirements/refactor-phase-1.md
@/Users/macbookpro/Desktop/Projects/curtains/.planning/phases/02-platform-portal/02-01-PLAN.md
@/Users/macbookpro/Desktop/Projects/curtains/.planning/phases/02-platform-portal/02-02-PLAN.md
</context>

<interfaces>
From src/modules/blog-management/hooks/use-blog-list.ts (Phase 1 current):
```typescript
export interface BlogPost {
  id: string;
  title: string;
  slug: string;
  content: string;
  status: 'draft' | 'published';
  featured_image: string;
  seo_title: string;
  seo_description: string;
  created_at: string;
}
```

From src/modules/dashboard/ structure (Phase 1):
Stats displayed: total leads, conversion rate, confirmed bookings, popular products, trends
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create blog API module (types, keys, requests, queries)</name>
  <files>
    - src/api/blog/types.ts
    - src/api/blog/keys.ts
    - src/api/blog/requests.ts
    - src/api/blog/queries.ts
    - src/api/blog/index.ts
  </files>
  <read_first>
  - src/api/categories/ (pattern template)
  - src/modules/blog-management/hooks/use-blog-list.ts (current BlogPost interface)
  - src/types/index.ts (for response types)
  </read_first>
  <action>
  Create blog API module by replicating category module structure with blog-specific types and endpoints:

  **src/api/blog/types.ts:**
  - IBlogPost interface with fields: id, title, slug, content, status, featured_image (optional), seo_title, seo_description, created_at, updated_at
  - BlogStatus enum: 'draft' | 'published'
  - GetBlogListParams with page, pageSize, sortBy, orderBy, search, status (optional)
  - CreateBlogParams with title, slug, content, status, featured_image (optional), seo_title, seo_description
  - UpdateBlogParams extends CreateBlogParams + { id }
  - DeleteBlogParams with id
  - GetBlogListResponse extends IPaginatedResponseType<IBlogPost[]>

  **src/api/blog/keys.ts:**
  - KEYS object with: BLOGS, BLOG_DETAIL, CREATE_BLOG, UPDATE_BLOG, DELETE_BLOG
  - Endpoint paths: 'admin/blog', 'admin/blog/:id', etc.

  **src/api/blog/requests.ts:**
  - getBlogList, getBlogDetail, createBlog, updateBlog, deleteBlog functions
  - Pattern: Exactly replicate category requests.ts

  **src/api/blog/queries.ts:**
  - useGetBlogList, useGetBlogDetail, useCreateBlogMutation, useUpdateBlogMutation, useDeleteBlogMutation hooks
  - Pattern: Exactly replicate category queries.ts

  **src/api/blog/index.ts:**
  - Barrel exports (4 lines)

  Use category module as reference.
  </action>
  <verify>
    <automated>grep -c "export const useGetBlogList\|export const useCreateBlogMutation\|export const useUpdateBlogMutation\|export const useDeleteBlogMutation\|export const useGetBlogDetail" /Users/macbookpro/Desktop/Projects/curtains/src/api/blog/queries.ts</automated>
  </verify>
  <done>Blog API module complete with all 5 hooks and 5 files</done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard stats API module (types, keys, requests, queries)</name>
  <files>
    - src/api/dashboard/types.ts
    - src/api/dashboard/keys.ts
    - src/api/dashboard/requests.ts
    - src/api/dashboard/queries.ts
    - src/api/dashboard/index.ts
  </files>
  <read_first>
  - src/api/categories/ (pattern template for hooks structure)
  - src/modules/dashboard/ (to understand stats structure)
  - src/types/index.ts (for response types)
  </read_first>
  <action>
  Create dashboard stats API module using TanStack Query pattern (queries only, no mutations):

  **src/api/dashboard/types.ts:**
  - IDashboardStats interface with fields:
    - totalLeads: number (leads this month)
    - totalLeadsThisWeek: number
    - totalLeadsToday: number
    - conversionRate: number (leads to bookings %)
    - confirmedBookings: number
    - popularProducts: Array<{ id: string; name: string; lead_count: number }>
    - leadsTrend: Array<{ date: string; count: number }> (last 7 days)
  - GetDashboardStatsResponse extends BaseResponseType<IDashboardStats>

  **src/api/dashboard/keys.ts:**
  - KEYS object with: DASHBOARD_STATS
  - Endpoint path: 'admin/dashboard/stats'

  **src/api/dashboard/requests.ts:**
  - getDashboardStats(signal?: AbortSignal): Promise<GetDashboardStatsResponse>
  - Call: httpInstance.get<GetDashboardStatsResponse>(KEYS.DASHBOARD_STATS, { signal })

  **src/api/dashboard/queries.ts:**
  - useGetDashboardStats(options?: Omit<UseQueryOptions<GetDashboardStatsResponse, Error>, 'queryKey'>)
  - Return: useQuery hook with queryKey [KEYS.DASHBOARD_STATS], queryFn getDashboardStats
  - Pattern: Match category useGetCategoryList structure

  **src/api/dashboard/index.ts:**
  - Barrel exports (4 lines)

  Dashboard is stats-only: no mutations (create/update/delete), only useGetDashboardStats query hook.
  </action>
  <verify>
    <automated>grep -c "export const useGetDashboardStats" /Users/macbookpro/Desktop/Projects/curtains/src/api/dashboard/queries.ts</automated>
  </verify>
  <done>Dashboard API module complete with stats query hook and 5 files</done>
</task>

<task type="auto">
  <name>Task 3: Verify TypeScript compilation for Wave 1 complete</name>
  <files>src/api/blog/, src/api/dashboard/</files>
  <read_first>
  - src/api/blog/index.ts (to verify barrel exports)
  - src/api/dashboard/index.ts (to verify barrel exports)
  - tsconfig.json
  </read_first>
  <action>
  Run TypeScript type-check to ensure all Wave 1 modules compile without errors:

  ```bash
  cd /Users/macbookpro/Desktop/Projects/curtains && npx tsc --noEmit
  ```

  This verifies:
  - All imports in blog and dashboard modules resolve correctly
  - All 6 API modules (categories, products, leads, bookings, blog, dashboard) compile together
  - No missing exports or type mismatches
  - Complete Wave 1 foundation is TypeScript-clean
  </action>
  <verify>
    <automated>cd /Users/macbookpro/Desktop/Projects/curtains && npx tsc --noEmit 2>&1 | grep -q "error" && exit 1 || exit 0</automated>
  </verify>
  <done>TypeScript compilation passes for Wave 1 complete with no errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | HTTP requests to /admin/blog, /admin/dashboard (authenticated) |
| API → Database | Blog posts and stats data from Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Injection | src/api/blog/requests.ts | mitigate | Use httpInstance with typed payloads; Markdown sanitization in rendering (Phase 3) |
| T-02-11 | Information Disclosure | src/api/dashboard/requests.ts | mitigate | Stats aggregates only; no PII exposed in statistics |
| T-02-12 | Authentication | Dashboard module | mitigate | HTTP instance middleware enforces session auth |

</threat_model>

<verification>
After task completion:
1. ✓ File src/api/blog/index.ts exports types, keys, requests, queries
2. ✓ File src/api/dashboard/index.ts exports types, keys, requests, queries
3. ✓ Blog module exports 5 hooks (CRUD mutations + queries)
4. ✓ Dashboard module exports 1 hook (stats query only)
5. ✓ All modules use httpInstance (no fetch keyword)
6. ✓ Pattern structure consistently matches category and product modules
7. ✓ npx tsc --noEmit passes with no errors
8. ✓ Wave 1 foundation complete: 6 API modules (categories, products, leads, bookings, blog, dashboard)
</verification>

<success_criteria>
Phase 2 Plan 03 is complete when:
- src/api/blog/ exists with complete module (types, keys, requests, queries, index)
- src/api/dashboard/ exists with complete module (types, keys, requests, queries, index)
- Blog module exports 5 hooks (full CRUD mutations)
- Dashboard module exports 1 stats query hook
- TypeScript compilation passes
- All 6 API modules (Wave 1) follow identical pattern structure
- Ready for Wave 2 to refactor containers and forms using these hooks
</success_criteria>

<output>
After completion, create `.planning/phases/02-platform-portal/02-03-SUMMARY.md`
</output>
