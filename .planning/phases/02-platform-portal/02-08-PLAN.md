---
phase: 02-platform-portal
plan: 08
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06, 02-07]
files_modified:
  - src/modules/lead-management/components/lead-edit-ui/lead-edit-form.tsx
  - src/modules/booking-management/components/booking-create-ui/booking-create-form.tsx
  - src/modules/booking-management/components/booking-edit-ui/booking-edit-form.tsx
  - src/modules/blog-management/components/blog-create-ui/blog-create-form.tsx
  - src/modules/blog-management/components/blog-edit-ui/blog-edit-form.tsx
autonomous: true
requirements: [LEAD-08, BOOK-01, BOOK-02, BLOG-01, BLOG-02, BLOG-03, BLOG-04, BLOG-05]
must_haves:
  truths:
    - "Lead edit form includes all editable fields (customer info, status, notes)"
    - "Booking create/edit forms include all fields (lead, date, time, status, notes)"
    - "Blog create/edit forms include complete fields with rich text content editor"
    - "All forms save all schema fields to database without truncation"
    - "Phase 2 form refactoring complete across all 6 admin modules"
  artifacts:
    - path: "src/modules/lead-management/components/lead-edit-ui/lead-edit-form.tsx"
      provides: "Complete lead form with customer and status fields"
      contains: "customer_name, customer_email, customer_phone, status, notes (5 fields)"
    - path: "src/modules/booking-management/components/booking-create-ui/booking-create-form.tsx"
      provides: "Complete booking form with date/time/status"
      contains: "lead_id, booking_date, booking_time, status, notes (5 fields)"
    - path: "src/modules/blog-management/components/blog-create-ui/blog-create-form.tsx"
      provides: "Complete blog form with rich text content editor"
      contains: "title, slug, content (rich text), status, featured_image, seo_title, seo_description (7 fields)"
  key_links:
    - from: "src/modules/blog-management/components/blog-create-ui/blog-create-form.tsx"
      to: "form-minimal-tiptap"
      via: "Rich text editor import"
      pattern: "import FormRichText from form-minimal-tiptap"
---

<objective>
Refactor remaining form components (lead, booking, blog) to include complete field coverage. This completes the Phase 2 form refactoring across all 6 admin modules.

Purpose: Each form must include all schema fields for admins to fully manage entities without workarounds.

Output:
- Lead edit form: Complete with customer info, status, notes (5 fields)
- Booking create/edit forms: Complete with lead, date, time, status, notes (5 fields each)
- Blog create/edit forms: Complete with rich text content, SEO fields (7 fields)
- All forms save all schema fields correctly
- Phase 2 form refactoring complete
</objective>

<execution_context>
@/Users/macbookpro/Desktop/Projects/curtains/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/macbookpro/Desktop/Projects/curtains/.planning/ROADMAP.md
@/Users/macbookpro/Desktop/Projects/curtains/.planning/phases/02-platform-portal/02-CONTEXT.md
@/Users/macbookpro/Desktop/Projects/curtains/docs/requirements/refactor-phase-1.md
</context>

<interfaces>
From Phase 1 schema definitions:

Lead schema:
```typescript
export interface Lead {
  id: string;
  customer_name: string;
  customer_email: string;
  customer_phone: string;
  status: 'new' | 'contacted' | 'closed';
  notes?: string;
  created_at: string;
}
```

Booking schema:
```typescript
export interface Booking {
  id: string;
  lead_id: string;
  booking_date: string;
  booking_time: string;
  status: 'pending' | 'confirmed' | 'done';
  notes?: string;
  created_at: string;
}
```

Blog schema:
```typescript
export interface BlogPost {
  id: string;
  title: string;
  slug: string;
  content: string;
  status: 'draft' | 'published';
  featured_image?: string;
  seo_title?: string;
  seo_description?: string;
  created_at: string;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor lead edit form with complete fields</name>
  <files>
    - src/modules/lead-management/components/lead-edit-ui/lead-edit-form.tsx
  </files>
  <read_first>
  - src/modules/lead-management/components/lead-edit-ui/lead-edit-form.tsx (current Phase 1)
  - src/types/database.ts (Lead type definition)
  - Lead schema above
  </read_first>
  <action>
  Update lead edit form to include all editable fields:

  **Required fields in lead edit form:**
  1. customer_name (text input, required, min 2 chars)
  2. customer_email (email input, required)
  3. customer_phone (phone input, required)
  4. status (select dropdown: 'new' | 'contacted' | 'closed', required)
  5. notes (textarea, optional, for admin notes about lead)

  **Form structure:**
  - Group 1: Customer Info (customer_name, customer_email, customer_phone)
  - Group 2: Lead Management (status, notes)

  Use existing form components (TextField, SelectField, TextAreaField) from src/components/form-field/.

  Pre-fill all fields from existing lead data. Do not allow editing id or created_at.

  Acceptance: Lead form renders all 5 fields, form submission saves all fields to database.
  </action>
  <verify>
    <automated>grep -c "customer_name\|customer_email\|customer_phone\|status\|notes" /Users/macbookpro/Desktop/Projects/curtains/src/modules/lead-management/components/lead-edit-ui/lead-edit-form.tsx</automated>
  </verify>
  <done>Lead form includes all 5 editable fields</done>
</task>

<task type="auto">
  <name>Task 2: Refactor booking create/edit forms with complete fields</name>
  <files>
    - src/modules/booking-management/components/booking-create-ui/booking-create-form.tsx
    - src/modules/booking-management/components/booking-edit-ui/booking-edit-form.tsx
  </files>
  <read_first>
  - src/modules/booking-management/components/booking-create-ui/booking-create-form.tsx (current Phase 1)
  - src/modules/booking-management/components/booking-edit-ui/booking-edit-form.tsx (current Phase 1)
  - Booking schema above
  - src/components/form-field/ (form field components)
  </read_first>
  <action>
  Update booking create/edit forms to include all fields:

  **Required fields in booking create/edit forms:**
  1. lead_id (select dropdown from leads list, required)
  2. booking_date (date picker, required, format YYYY-MM-DD)
  3. booking_time (time picker or HH:MM text input, required)
  4. status (select dropdown: 'pending' | 'confirmed' | 'done', required)
  5. notes (textarea, optional, for booking notes)

  **Form structure:**
  - Group 1: Booking Details (lead_id, booking_date, booking_time)
  - Group 2: Status & Notes (status, notes)

  Use existing form components (SelectField, TextField, TextAreaField) from src/components/form-field/.

  For create form: lead_id as required select dropdown populated from useGetLeadList hook.
  For edit form: pre-fill all fields from existing booking data. Allow editing all fields.

  Acceptance: Booking forms render all 5 fields, form submission saves all fields to database.
  </action>
  <verify>
    <automated>grep -c "lead_id\|booking_date\|booking_time\|status\|notes" /Users/macbookpro/Desktop/Projects/curtains/src/modules/booking-management/components/booking-create-ui/booking-create-form.tsx</automated>
  </verify>
  <done>Booking forms include all 5 fields</done>
</task>

<task type="auto">
  <name>Task 3: Refactor blog create/edit forms with rich text content editor and SEO</name>
  <files>
    - src/modules/blog-management/components/blog-create-ui/blog-create-form.tsx
    - src/modules/blog-management/components/blog-edit-ui/blog-edit-form.tsx
  </files>
  <read_first>
  - src/modules/blog-management/components/blog-create-ui/blog-create-form.tsx (current Phase 1)
  - src/modules/blog-management/components/blog-edit-ui/blog-edit-form.tsx (current Phase 1)
  - docs/requirements/refactor-phase-1.md section D (form-minimal-tiptap pattern)
  - Blog schema above
  - src/components/form-field/ (form field components)
  </read_first>
  <action>
  Update blog create/edit forms to include complete fields with rich text editor:

  **Required fields in blog create/edit forms:**
  Basic Information:
  1. title (text input, required, min 5 chars)
  2. slug (text input, required, regex: lowercase-with-hyphens)
  3. status (select dropdown: 'draft' | 'published', required)

  Content:
  4. content (FormRichText using form-minimal-tiptap, required, min 50 chars) — CRITICAL: Use FormRichText for markdown/rich text

  Featured Image:
  5. featured_image (file dropzone for thumbnail image, optional, single file)

  SEO:
  6. seo_title (text input for SEO, optional, min 10 chars)
  7. seo_description (textarea for SEO, optional, min 20 chars)

  **Form structure:**
  - Group 1: Basic (title, slug, status)
  - Group 2: Content (content with rich text editor using form-minimal-tiptap)
  - Group 3: Featured Image (image upload with preview)
  - Group 4: SEO (seo_title, seo_description)

  **Key change:** Use FormRichText for blog content (not textarea):
  ```typescript
  <FormRichText
    control={control}
    name="content"
    label="Content"
    placeholder="Enter blog post content (supports Markdown)"
    required
    disabled={isPending}
  />
  ```

  Use existing form components (TextField, SelectField, TextAreaField, FormRichText) from src/components/form-field/.

  For edit form, pre-fill all fields from existing blog data. Content field should display rich text in editor.

  Acceptance: Blog forms render all 7 fields with rich text content editor, form submission saves all fields to database.
  </action>
  <verify>
    <automated>grep -q "FormRichText\|form-minimal-tiptap" /Users/macbookpro/Desktop/Projects/curtains/src/modules/blog-management/components/blog-create-ui/blog-create-form.tsx && grep -c "title\|slug\|content\|status\|featured_image\|seo_title\|seo_description" /Users/macbookpro/Desktop/Projects/curtains/src/modules/blog-management/components/blog-create-ui/blog-create-form.tsx</automated>
  </verify>
  <done>Blog forms include 7 fields with form-minimal-tiptap for rich text content</done>
</task>

<task type="auto">
  <name>Task 4: Verify TypeScript compilation and Phase 2 form refactoring complete</name>
  <files>src/modules/lead-management/, src/modules/booking-management/, src/modules/blog-management/</files>
  <read_first>
  - All refactored form files
  - src/components/form-field/ (form components used)
  - tsconfig.json
  </read_first>
  <action>
  Run TypeScript type-check to verify all form refactoring complete:

  ```bash
  cd /Users/macbookpro/Desktop/Projects/curtains && npx tsc --noEmit
  ```

  This verifies:
  - All form field imports resolve (TextField, SelectField, TextAreaField, FormRichText)
  - All lead, booking, blog form files compile without errors
  - Rich text editor imports work in blog forms
  - Complete Phase 2 form refactoring (Plans 07-08) is TypeScript-clean
  - All 6 admin modules (category, product, lead, booking, blog, dashboard) are complete
  </action>
  <verify>
    <automated>cd /Users/macbookpro/Desktop/Projects/curtains && npx tsc --noEmit 2>&1 | grep -q "error" && exit 1 || exit 0</automated>
  </verify>
  <done>TypeScript compilation passes for all form refactoring</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input → Form Validation | Zod schema validates all form inputs before submission |
| Rich Text Editor → Content | form-minimal-tiptap sanitizes markdown/rich text content |
| Form → Database | API sends validated form data to backend RLS-protected endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-24 | Injection | Blog content field | mitigate | form-minimal-tiptap sanitizes rich text; backend validates input |
| T-02-25 | Injection | Lead/Booking/Blog form fields | mitigate | Zod schema validation on client; backend RLS policies enforce constraints |
| T-02-26 | Information Disclosure | Lead phone/email fields | mitigate | Admin-only forms; backend RLS restricts access; customer data not exposed to public |

</threat_model>

<verification>
After task completion:
1. ✓ Lead form includes 5 editable fields (customer_name, email, phone, status, notes)
2. ✓ Lead form saves all fields to database
3. ✓ Booking forms include 5 fields (lead_id, booking_date, booking_time, status, notes)
4. ✓ Booking forms pre-fill from existing data on edit
5. ✓ Blog forms include 7 fields (title, slug, content [rich text], status, featured_image, seo_title, seo_description)
6. ✓ Blog content field uses FormRichText (not textarea)
7. ✓ All forms compile without TypeScript errors
8. ✓ npx tsc --noEmit passes
9. ✓ Phase 2 form refactoring complete (all 6 modules: category, product, lead, booking, blog, dashboard)
</verification>

<success_criteria>
Phase 2 Plan 08 is complete when:
- Lead forms include complete field coverage (5 fields)
- Booking forms include complete field coverage (5 fields each)
- Blog forms include complete field coverage (7 fields including rich text content)
- All description/content fields use form-minimal-tiptap rich text editor
- All forms render without errors and save all fields to database
- TypeScript compilation passes
- Phase 2 refactoring complete: API modules + containers + forms all follow new patterns across all 6 admin modules
</success_criteria>

<output>
After completion, create `.planning/phases/02-platform-portal/02-08-SUMMARY.md`
</output>
